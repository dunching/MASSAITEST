# MassAI Crowd Demo 状态恢复

## 当前分支与任务

[COMPUTED][HIGH] 工程：`E:\Projects\SuperInvincibleTank_MASSAITEST`；分支：`codex/t5-target-region-transport`；最新已推送checkpoint为`25029f577202d6cc78370ac489fd5b4e5118e696`。第十与第十一切片代码、测试与本文档更新尚未提交。

[COMPUTED][HIGH] 插件阶段2纯算法迁移已经完成；阶段3的Runtime trait/fragments、Shared Flow、Target Region、Guidance Compose、Local Predictive、Particle Safety、Facing与最终Movement/稳定Merge/Commit生产切换也已完成。阶段4单boundary Gather前十一切片已覆盖基础snapshot、Guidance overlay、Compose→Local Predictive→MovementPredict→Particle/Obstacle→Facing→MovementFinalize输入链、Finalize最小写入、独立post-finalize采集职责、T1唯一runtime、Combat/Visual最终rollback完成门及六个Demo迁移镜像物理删除；100/500、自由游荡和新战斗能力不在当前阶段。

## 已实现

[COMPUTED][HIGH] TargetApproach、TargetSlotLayout与旧Polar Density已删除；唯一Guidance Compose、资源引用rollback、异构Target标记和11阶段性能归类已接入。

[COMPUTED][HIGH] Guidance Compose、Local Predictive、MovementPredict、Particle与Facing已使用不可变POD在WORK线程求解，GT等待后写回；Target/Business、T1、诊断和累计器仍为分阶段GT准备或采集，尚未达到整boundary一次Mass读取。

[COMPUTED][HIGH] `MassCrowdSimulation`阶段1已建立五模块与公共合同；Shared Flow、Target Region Transport、Guidance Compose、Velocity Half-Plane、Local Predictive、Particle Safety与Facing已提取到`MassCrowdCore`并通过旧/Core独立等价fixture。上述运动阶段生产processor均已切到Runtime WORK；Demo保留场景Gather、Round指标、诊断、rollback协调及最终RoundSim checkpoint状态，不再保留六个逐阶段Runtime兼容镜像。Combat RoundSim状态hash继续属于Demo适配层。

[COMPUTED][HIGH] `MassCrowdRuntime`已建立Base Movement trait/fragments和稳定Gather/Merge/Commit Bridge；正式Guidance Compose、Local Predictive、Particle Safety及最终Movement组装均由Runtime WORK执行。MovementFinalize在完整身份预验证后同步发布Runtime movement/state与Demo checkpoint镜像，Authority/Client Commit从Runtime MovementOutput写Engine状态。

[COMPUTED][HIGH] T1测试布局切换已从普通视觉不连续中分离：8772中`non_correction=0`、test boundary reset=21、round reset=20。

## 最新验证

[COMPUTED][HIGH] `git diff --check`、Development、DebugGame（均使用`-DisableUnity`）、当前105/105项`CrowdDemo`自动化与13/13项`MassCrowd`插件自动化通过；SF1 golden hash保持`267519150`。默认Unity Development仍因插件旧`.cpp`匿名辅助函数重名失败，本切片未修改这些文件。

[COMPUTED][HIGH] 新增验证目录：T7普通运行=`8781/8783`，无shader DDC控制失败=`8782`，T5M稳定诊断=`8785`，T6M旧结果=`8787`，60秒时间反证=`8788`，生命周期归因=`8789`，单向Hold修复复测=`8790`。

[COMPUTED][HIGH] 最新fixed-step p95依次为T1 `1.131ms`、T2 `3.073ms`、T3 `2.938ms`、T4 `3.376ms`、T5S `5.362ms`、T6A `3.114ms`、T6S `4.261ms`、T7 `1.739ms`、T8 `1.598ms`。

## 最新性能与能力事实

[COMPUTED][HIGH] 客户端性能窗口已对齐Round 1，并新增Game/Render/GPU、shader/loading/PSO与独立warmup证据。T7 8781/8783 frame p95=`6.016/5.820ms`且Round内资源活动为0；8782因shader jobs超过60秒未ready，不能作为8777唯一根因。

[COMPUTED][HIGH] T6M 8788延长到60秒仍只有最终16/20，排除单纯时间不足；8789中634个具备继承资格的claim全部迁移，`dropped_still_feasible=0`，排除claim迁移丢失。

[COMPUTED][HIGH] RoundResultHeader contract v2已关闭运行时门：8790异构payload为1970字节，无Native NetSerialize Warning。`EngagedHold`现只抑制目标靠近造成的主动后退，保留切向与目标远离跟随。

[COMPUTED][HIGH] 8790 Round末inside/coverage=`20/20`，安全、同步、correction与性能通过。用户确认AcquireThenHold资格有效期间不要求持续重排Region；最后90步terminal population最低`18/20`、coverage最低`17/20`、settled window=0保留为过程诊断，T6M技术放行。

## 首失败与下一步

[COMPUTED][HIGH] 当前统一输入第三切片没有新增生产首失败：Development、DebugGame、`CrowdDemo` 102/102与`MassCrowd` 12/12均通过。历史首次Particle WORK编译失败与测试自引用失败均已修正，生产回归没有复现这些测试/编译问题。

[COMPUTED][HIGH] 8662 T2 Small正式Round 1验证通过：handoff/inside/settled=`20/20/20`、Region coverage=`16/16`、Particle Hard/Swept/Obstacle/Bounds与invalid/fallback均为0，candidate/applied hash双端一致；该运行未进行人工审片。

[COMPUTED][HIGH] 最终Movement/Commit接管后，Development、DebugGame、Runtime定向2/2、`MassCrowd` 11/11与`CrowdDemo` 102/102通过。8663 T2保持handoff/inside/terminal=`20/20/20`、coverage=`16/16`、安全与同步通过，fixed-step/Commit p95=`3.529/0.021ms`；8664 SF1 Single authority短运行无VIOLATION。首次定向自动化失败来自测试代码把数组自身元素直接传给同数组`Add`，触发UE容器自引用断言；改为值复制后通过，生产路径不是该失败来源。

[COMPUTED][HIGH] Facing生产WORK迁移后，Development、DebugGame、Facing定向2/2、`MassCrowd` 11/11与`CrowdDemo` 102/102通过。8665 T2保持20/20 terminal、16/16 coverage、双端Yaw correction误差0，fixed-step/Commit p95=`3.638/0.020ms`；8666 SF1无Particle authority路径无VIOLATION。

[COMPUTED][HIGH] Shared Flow生产WORK迁移后，Development、DebugGame、Runtime定向2/2、Shared Flow旧/Core/Runtime等价1/1、`MassCrowd` 11/11与`CrowdDemo` 102/102通过。8667 T2保持20/20 terminal、16/16 coverage、安全与双端hash通过，fixed-step/Flow p95=`3.166/0.264ms`；8668 SF1确认hash=`267519150`、rebuild=1且无硬错误。

[COMPUTED][HIGH] Target Region生产WORK迁移后，Development、DebugGame、Runtime定向1/1、旧/Core/Runtime等价1/1、`MassCrowd` 12/12与`CrowdDemo` 102/102通过。8669 T2保持20/20 terminal、16/16 coverage、五类hash和性能门；8671异构T6 Static保持inside-band/coverage=`20/20`、unrouted/invalid/validation failure=0，安全、同步与性能门通过。两次production运行的correction均为零误差快速路径，没有实际rewind replay。

[COMPUTED][HIGH] 单boundary基础Gather第一切片完成：新增Runtime稳定snapshot和Demo boundary Gather processor；Shared Flow与Target Demand复用同一基础快照，Flow candidate通过prepared SoA传递。Development、DebugGame、`MassCrowd` 12/12、`CrowdDemo` 102/102通过。8672 T2保持20/20 terminal、16/16 coverage，fixed-step p95=`3.599ms`；8673异构T6保持inside/coverage=`20/20`，fixed-step p95=`4.844ms`，安全与双端hash通过。8676 SF1完成多轮并保持flow hash=`267519150`、goal/wall/corridor/turn=`1/1/1/1`；Run脚本因SF1不输出SoftPressure性能checkpoint而超时，该超时不是模拟失败。

[COMPUTED][HIGH] Guidance overlay与Local Predictive统一输入第二切片完成：Runtime Bridge将snapshot与Flow/Target/Business candidate稳定合并，Compose和Local Predictive不再为WORK输入重复读取基础Mass fragments。Development、DebugGame、`MassCrowd` 12/12、`CrowdDemo` 102/102通过；8677 T2保持20/20 terminal、16/16 coverage，fixed-step p95=`3.854ms`；8678异构T6保持inside/coverage=`20/20`，fixed-step p95=`5.265ms`，安全、同步与性能门通过。8680 SF1烟雾确认hash=`267519150`及完整Flow→Compose→Finalize→Commit首步链，无VIOLATION；该短运行未完成整轮。

[COMPUTED][HIGH] MovementPredict、Particle与Facing统一输入第三切片完成：新增Runtime MovementPredict POD WORK，Particle从snapshot+prepared predict构造Core输入，Facing从snapshot+prepared composed/particle构造Core输入。Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102通过；8681 T2保持20/20 terminal、16/16 coverage，fixed-step p95=`5.379ms`；8682异构T6保持inside/coverage=`20/20`，fixed-step p95=`6.598ms`，两者安全、同步与性能门通过。8683 SF1 smoke保持flow hash=`267519150`、rebuild=1且无硬错误。

[COMPUTED][HIGH] MovementFinalize统一输入第四切片完成：Particle/SF1 Obstacle与Facing发布prepared最终事实，Runtime helper从snapshot组装Commit输入；旧第一遍全实体Gather删除，写前完整身份和prepared/Runtime/Demo镜像原子门保留。Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102通过；8684 T2保持20/20 terminal、16/16 coverage，fixed-step p95=`4.393ms`；8685异构T6保持inside/coverage=`20/20`，fixed-step p95=`5.438ms`；8686 SF1 smoke保持flow hash=`267519150`、rebuild=1且无硬错误。

[COMPUTED][HIGH] MovementFinalize查询职责第五切片完成：写前镜像/身份门与提交后业务/指标读取拆为`ValidationQuery`和`ApplyMetricsQuery`；后者不再读取MoveIntent、Runtime properties、Runtime Particle/Facing。Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102通过；8687 T2保持20/20 terminal、16/16 coverage，fixed-step p95=`4.149ms`；8688异构T6保持inside/coverage=`20/20`，fixed-step p95=`5.683ms`；8689 SF1 smoke保持flow hash=`267519150`、rebuild=1且无硬错误。

[COMPUTED][HIGH] 第六切片已把Finalize写入与业务/诊断采集拆为独立processor，并增加每boundary Finalize成功标记门控post-finalize及Authority/Client Commit。Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102通过；8693 T2保持20/20 terminal、16/16 coverage，fixed-step p95=`4.074ms`；8694异构T6保持completed/settled/inside/coverage=`20/20`，fixed-step p95=`5.073ms`；8695 SF1 smoke保持flow hash=`267519150`、rebuild=1且无硬错误。

[COMPUTED][HIGH] 第七切片已删除post-finalize对Formation、Composed Guidance、Particle Properties和未使用Particle Constraint fragment的读取。8697首次异构回归暴露rollback Radius语义替代错误；修正为boundary formation fact保存精确Radius后，8698 T6 rollback hit/miss/mismatch=`80/0/0`、inside/coverage=`20/20`、fixed-step p95=`6.540ms`，8699 T2为terminal/inside=`20/20`、coverage=`16/16`、rollback=`54/0/0`、fixed-step p95=`4.322ms`，8702 SF1 hash=`267519150`、rebuild=1。Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102均通过。

[COMPUTED][HIGH] 第八切片已删除post-finalize对FlowSample和ObstacleConstraint fragment的读取；8703异构T6 rollback hit/miss/mismatch=`80/0/0`、inside/coverage=`20/20`、fixed-step p95=`4.595ms`，8704 SF1 hash=`267519150`、rebuild=1且无硬错误。最终Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102通过。

[COMPUTED][HIGH] 第九切片已删除post-finalize对GuidanceCandidates和Facing fragment的读取；Guidance由boundary snapshot与三类prepared overlay经Runtime Bridge重建，Facing连续settle与最终资格由Facing阶段发布精确rollback fact。8705异构T6 rollback hit/miss/mismatch=`80/0/0`、inside/coverage=`20/20`、fixed-step p95=`4.551ms`且双端无硬错误；8706 SF1 hash=`267519150`、rebuild=1且无硬错误。Development、DebugGame、`MassCrowd` 12/12与`CrowdDemo` 102/102通过。

[COMPUTED][HIGH] 第十切片已把T1 OpenSpawn收敛为Pipeline唯一runtime与prepared boundary facts，物理删除per-agent fragment；Combat/Visual最终事实由VisualStateResolve完成，movement/combat双完成门控制snapshot replay/checkpoint。PostFinalize只读取Identity与最终RoundSim。Development与DebugGame（`-DisableUnity`）、T1 4/4、Combat 15/15、结构1/1、`MassCrowd` 12/12与`CrowdDemo` 105/105通过。

[COMPUTED][HIGH] 8707 T1保持reset/insert/remove/settling与Particle安全合同，rollback=`53/0/0`、fixed-step p95=`1.851ms`；8709异构T6保持inside/coverage=`20/20`、rollback=`53/0/0`、fixed-step p95=`6.334ms`；8708 T7和8710 T8无安全、同步或snapshot完整性错误，T8三类业务hash双端一致；8714 SF1 smoke保持hash=`267519150`、rebuild=1但未完成整轮。

[COMPUTED][HIGH] 第十一切片已物理删除六个Demo迁移运动fragment、Mass模板注册/初始化、processor读写、派生rollback副本和旧`BuildGatherRecord()`；Runtime Facing现在持有连续settle状态。Development、DebugGame、结构/适配器定向测试、`CrowdDemo` 105/105与`MassCrowd` 12/12通过。8722 T2为terminal/inside=`20/20`、coverage=`16/16`、fixed-step/client frame p95=`4.028/5.248ms`；8717异构T6为completed/settled=`20/20`、fixed-step/client frame p95=`4.556/5.481ms`；8718 T1=`1.641ms`，8719 T8=`2.025ms`且业务hash双端一致；8721 SF1 smoke保持hash=`267519150`、rebuild=1。

[COMPUTED][HIGH] 第十二切片已把Compose→Local Predictive→MovementPredict合并为一次GT准备、一次ThreadPool dispatch和一次原子发布；旧三个processor实现已物理删除。Development、DebugGame（均`-DisableUnity`）、`CrowdDemo` 105/105与`MassCrowd` 13/13通过。8723 T2保持terminal/inside=`20/20`、coverage=`16/16`、fixed-step p95=`3.849ms`；8724异构T6保持completed/settled/inside/coverage=`20/20`、fixed-step p95=`5.337ms`；8725 T1为`1.649ms`；8726 T8为`2.131ms`且业务hash双端一致。四次运行均为agents/visible=`20/20`、correction误差0且无硬错误。

[INFERRED][HIGH] 下一步只继续收敛Particle、Facing、Finalize周围剩余GT接缝；完整boundary单次Mass读取和按能力archetype仍未完成，100/500不在当前放行范围。

## 提交边界

[COMPUTED][HIGH] 本checkpoint包含当前工作区全部架构收敛代码、自动化、脚本删除和文档；不包含Saved运行输出、缓存或工具二进制。

[COMPUTED][HIGH] 本轮未修改地图、Lighting、30Hz、Particle参数或网络预算；未执行reset、checkout、clean或stash。

[RULES I BROKE]：[COMPUTED][HIGH] 无。
