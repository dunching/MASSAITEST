# MassAI Crowd Demo 状态恢复

## 2026-08-03 Git 提交恢复点

[COMPUTED][HIGH] 本恢复点位于分支 `codex/t5-target-region-transport`，提交前基线为 `49bbf5d feat: establish worker authority runtime foundation`；本次按用户明确要求纳入当前工作区全部 92 项累计改动，范围为 11,778 行新增、6,151 行删除，并包含 Legacy Boundary API/测试物理删除、WA7-R 网络合同、WA8 Dirty Apply/Checkpoint 收敛及 WA8.5 Work/Timer/Spatial/Target/Particle 当前实现。

[COMPUTED][HIGH] 提交前最终验证为 Development Editor DisableUnity 构建成功、`MassCrowd.Runtime` 68/68、`MassCrowd.RuntimeV2` 33/33、TargetRegion 2/2、Core Particle 6/6、Particle Plugin Core Equivalence 1/1；`git diff --check` 无内容错误，只有仓库现有 LF→CRLF 提示。

[INFERRED][HIGH] 提交后的首要开放项仍是 Particle 大型单闭合 Island 的 Cell-Pair Owner/逐轮 Barrier 分片与高密度 10k 微基准；随后关闭 T5 step 886 Target 长窗口缺陷，再进入 WA9 三个 10k 真实规模场景。不得依据本提交中已完成的多 Island 分解声称大型单 Island Cell Solver 已关闭。

## 2026-08-03 WA8.5 Target Cohort 与 Particle Island 恢复点

[COMPUTED][HIGH] Target Runtime 已把 `TargetControl` 全资源工作拆成稳定编码的 Cohort Work；实体 Movement/Facing 依赖只回灌所属 Cohort。静态 Objective 不再每个 Clock Tick 无条件调度全部 Cohort，只有非零 TargetVelocity 保留全 Cohort Clock；Objective/TargetControl Revision 仍显式调度受影响工作。

[COMPUTED][HIGH] 新增 10k 双 Cohort 回归：基线为 2×5000 实体，随后只执行第二 Cohort；Guidance 增量严格为 `ceil(5000/128)=40` 个 shard，未受影响 Cohort 无 Dirty Patch、无 Topology rebuild，依赖闭包为 `5000+3`。DisableUnity、RuntimeV2 33/33、TargetRegion 2/2 通过。

[COMPUTED][HIGH] Particle Work 已从无条件完整 Agent Solver 改为保守修正半径扩张后的闭合 Interaction Island 分解；Island 按最小 AgentId 稳定排序、独立求解并按 AgentId/PairKey 稳定归并。输出新增 Island/Cell/Cross-cell/最大 Island 遥测，全局 Applied-State exact validation 失败时 fail-closed 回退单体 Solver。闭合 Island 逆序回归、Core Particle 6/6、RuntimeV2 33/33 与 Plugin Core Equivalence 通过。

[INFERRED][HIGH] Particle 尚未完成大 Island 的 Cell Barrier 求解：当前 Cell 只提供稳定 ownership/复杂度统计，单个大型闭合 Island 仍由单次 Kernel Solver 处理。下一切片必须实现跨 Cell Pair 的字典序 Owner、每轮约束 Barrier 与稳定实体归并，再补高密度 10k Island 微基准；不得把当前 Island 分解误报为完整 Particle Cell 分片关闭。

## 2026-08-03 WA8.5 Work Ring / Time Wheel / Spatial 复杂度门恢复点

[COMPUTED][HIGH] 生产实现审计确认 Work Ring 已是 `Priority × Domain` 固定分桶、桶内稳定排序/消费游标和持久去重索引；`PopCurrent` 只探测固定数量桶，累计 `WorkPopBucketProbeCount`。Time Wheel 已使用最小 Tick Heap；`DrainDue` 只弹出到期 Tick，并累计 `TimeWheelScannedBucketCount`。本切片没有重复重写这两套已经满足合同的数据结构。

[COMPUTED][HIGH] Spatial Index 已提供 `Spawn/Despawn/UpdateEntity` 增量接口，Movement Dirty 只有跨越 400cm Cell 时才迁移成员；Runtime 遥测分别报告 full rebuild、incremental update 与 cell migration。复杂度回归已从 100 实体单一比例强化为 10k×1% 和 10k×10% 两组，均要求 rebuild=`0`、incremental=`20000`、migration 精确等于 `100/1000`。

[COMPUTED][HIGH] Development Editor DisableUnity 成功；`MassCrowd.RuntimeV2.Complexity` 3/3 与完整 `MassCrowd.RuntimeV2` 32/32 通过，覆盖 1k/2k/5k/10k Work Drain、10k 稀疏 Wakeup 和两组 10k Spatial Migration。测试启动阶段仍有两条项目既有通用 `Condition failed`，全部目标测试均明确返回成功。

[INFERRED][HIGH] WA8.5 的 Work Ring、Time Wheel 与 Spatial 子项已经关闭。下一项是审计 Target 的 Cohort Plan/128 Entity Guidance 是否在生产路径按受影响范围失效，并为 Particle 补真正的闭合 Island/Cell 分片；当前 Particle Worker 仍把完整 Agent 数组交给单次 Solver，不能误报为 Island 分片完成。

## 2026-08-03 WA8 客户端 Legacy Round 中间诊断物理删除恢复点

[COMPUTED][HIGH] `RecordRoundResultComparisonAndApplied` 中 902 行无生产者客户端 Round 中间比较已物理删除，包括 Dynamic Flow、Particle/Guidance、T1–T6、Projectile、Target Stability、Route 与 Failure Fixture 的本地/服务端哈希比较；`bLegacyRoundDiagnosticsProduced`、Compare 缓存中的 `ParticleMetrics`/`ServerClientParticleHashMatch` 及其旧测试断言也已删除。生产源码不再包含 `CrowdDemoLegacyRoundDiagnostics` 或上述 `role=client` 中间哈希日志。

[COMPUTED][HIGH] 客户端仍保留独立的 Checkpoint 状态比较：完整组装后记录 Position/Velocity/Yaw/Overlap 误差，并保留 Worker Digest 与稀疏 Correction 作为普通预测一致性门。Checkpoint 中的服务端性能/场景汇总字段仍有独立服务端报告消费者，因此本切片没有把通用 Checkpoint 汇总载荷误删为“Legacy 客户端比较字段”。

[COMPUTED][HIGH] Development Editor DisableUnity、`CrowdDemo.Architecture.PostFinalizeMinimalQuery` 1/1、`CrowdDemo.SF.Transport.RoundCheckpoint` 2/2 通过。9809 的真实双进程 Correction-off 场景完成一次 Checkpoint queue/apply，Position/Velocity/Yaw 误差均为 0，客户端继续到 Epoch/Input=`221/307`，`runtime_v2_failure=0`；Legacy 客户端日志与硬失败匹配数均为 0。

[INFERRED][HIGH] WA8 的该删除项已经关闭。下一切片进入 WA8.5：先审计并基准 Work Ring 与 Time Wheel 的扫描复杂度，再做 Spatial 增量迁移；Target >900 Tick 长窗口缺陷仍必须在 WA9 三个 10k 场景前关闭。

## 2026-08-03 WA7-R/WA8 专用 Round Checkpoint 与稀疏 Correction 恢复点

[COMPUTED][HIGH] 旧 `FCrowdDemoCorrectionFrame/Header/Chunk`、普通完整 Correction producer/consumer、分块 RPC 语义和 `ShouldBuildCorrectionFrame` 已从生产源码删除。终局状态现在使用独立的 `FCrowdDemoRoundCheckpointFrame/Header/Chunk`；客户端完整组装后在下一有序 Owner Boundary 应用，不再等待已退休的 Round `SimulatedServerTimeSeconds`。9803 的 6 秒双进程场景得到 `pipeline_queued=1`、`boundary_applied=1`、Position/Velocity/Yaw 误差均为 0，且 Correction-off 时 Runtime 持续推进。

[COMPUTED][HIGH] Worker 可靠 RPC 分块上限已从 48 KiB 收敛为共享的 4 KiB 安全常量；49 KiB 回归被拆为 13 块。该修复关闭了 9793 的 52 个 reliable partial bunch 导致连接断开的真实缺陷。`RoundCheckpointChunks`、`RoundCheckpointTerminalTolerance`、`WorkerPacketTransport`、`WorkerCodec` 与 `PostFinalizeMinimalQuery` 均通过。

[COMPUTED][HIGH] 默认稀疏 Correction 在真实 Combat Digest 处暴露并关闭了 revision 传播缺陷：调度器此前未给 `FCrowdWorkerDomainContext::CorrectionRevision` 赋值，修补后的 Combat 输出以 revision 0 覆盖权威 revision 1，被 Dirty Store 正确拒绝。现在 Context 继承 `LastAppliedAuthorityCorrectionSequence` 并与 Stage Work 水位取最大；`SparseCorrectionWithoutWorldRebuild` 新增有序 Barrier、Lifecycle→Behavior 依赖传播、后续 30 Tick 与下一 Digest 全匹配门。

[COMPUTED][HIGH] 9807 的 30 秒真实双进程场景在 Digest 16/19/20/27/28 多次发现 Combat Scope mismatch 并完成可靠稀疏修补；客户端随后继续到 Epoch 905、Input 991，`runtime_v2_failure=0`、无 Runtime restart，终局 Round Checkpoint 正常应用。已退休的客户端 Round 中间哈希没有生产者，现明确记录 `available=0`，不再把 FNV 初值与服务端诊断结果比较成伪 `VIOLATION`；有效一致性门是 Worker Digest、Correction 遥测和 Checkpoint 前后状态误差。

[INFERRED][HIGH] WA8 仍 OPEN，但专用 Checkpoint/普通完整 Correction 删除这一切片已经关闭。下一步应物理移除仅剩的客户端 Legacy Round 中间诊断计算与对应测试/载荷字段，确认 Round Plan/Checkpoint 边界之外没有完整 Mass Query；随后执行 WA8.5 的 Work Ring、Time Wheel、Spatial、Target/Particle 微基准，并在进入 WA9 10k 前关闭 Target 超过 900 Tick 的长窗口缺陷。

## 2026-08-03 WA8 Legacy 普通完整 Correction 关闭恢复点

[COMPUTED][HIGH] `UCrowdDemoRoundSimPipelineSubsystem::ShouldBuildCorrectionFrame` 现在默认拒绝旧 Round 完整纠错；只有显式 `-CrowdDemoLegacyFullCorrectionDiagnostic` 才能重新启用诊断帧。普通 Epoch 不再构造或发送 `FCrowdDemoCorrectionFrame::AgentStates`，权威纠错继续由 WA7-R Digest/Scope Correction 通道负责。

[COMPUTED][HIGH] RoundResult 与 Late Join/Resync Checkpoint 仍使用独立发布门，不受普通纠错关闭影响。9791 全 Production T8 在 900 batch 后只发布 1 个 `plan_phase=1.000/source_checkpoint_revision=1` 的终局完整 Checkpoint；此前每约 0.5 秒发布完整世界的路径已归零。

[COMPUTED][HIGH] Development Editor DisableUnity、`PostFinalizeMinimalQuery`、`WorkerCodec` 与 `WorkerResultApply.LifecycleOwnerAndEvents` 三项定向自动化通过。默认单进程双 PIE 1/1 通过 300 Epoch 无纠错预测和稀疏恢复断言，且旧 server full frame/client full-frame header 均为 0。9790 T5 达到 batch 600、Target mode/verified=`2/20`、stale lifecycle=`0`；9791 T8 达到 batch 900、Ordered Event=`150`、Golden Hash=`439379904/1411313634/6141440`，两场均无硬失败。

[INFERRED][HIGH] WA8 仍未整体关闭：旧 `FCrowdDemoCorrectionFrame` 类型、分块 RPC 与客户端完整帧消费仍作为显式诊断/Checkpoint 共用外壳存在。下一切片应先把 RoundResult/Late Join 迁到专用 Checkpoint 载荷，再物理删除 Legacy 普通 Correction producer/consumer/RPC；随后关闭 9780 的 Target step 886 长窗口缺陷，才进入 WA8.5 微基准与 WA9 10k 场景。

## 2026-08-03 WA8 唯一 Commit / 按需 Checkpoint 恢复点

[COMPUTED][HIGH] `PreparedMovementCommitPlan`、`FCrowdDemoPreparedPostFinalizeAgentRecord` 和 `PreparedCheckpointAgentStates` 已从生产类型物理删除。Worker Movement Boundary Commit 成为该 Tick 唯一完整 Commit owner，并保留到 PostFinalize、Authority/Client Commit 记账和 Checkpoint Publisher 全部消费完成；`FinishFixedStep` 后统一释放 Movement/Combat Commit。

[COMPUTED][HIGH] Result Apply 不再为全部成员构造 Final Business、PostFinalize 和 Checkpoint 副本。最终 Business 只按 `CurrentStepMassDirtyEntityRefs` 构造并写入 Dirty Mass Collection；PostFinalize 直接消费唯一 Commit，Checkpoint State 只在 `ShouldBuildCorrectionFrame/ShouldBuildRoundResult` 发布门通过后临时序列化。结构门拒绝三个已删除缓存以及 Apply 内 Checkpoint 数组构造。

[COMPUTED][HIGH] Development Editor DisableUnity 成功；`PostFinalizeMinimalQuery`、`WorkerResultApply.LifecycleOwnerAndEvents`、`GatherMergeCommit` 与 T8 单元组共 17 项自动化通过。9785 T5 到 batch 600，stale lifecycle=`0`；9786 T8 到 batch 900，事件=`50/50/50/50/50`、duplicate/expired=`0/0`、Golden Hash=`439379904/1411313634/6141440`。两场均未出现 `VIOLATION`；日志中的 `rejected_echo=20` 是既有 Movement echo 计数，不是提交拒绝。

[INFERRED][HIGH] WA8 仍 OPEN。Demo `ShouldBuildCorrectionFrame` 目前仍周期性把完整 `States` 写入 `FCrowdDemoCorrectionFrame::AgentStates`，9786 第一轮发布了 55 个完整 Correction Frame；Demo-local Round DAG 与完整 `BoundarySnapshot` 也仍存在。下一步应把该 Legacy 完整 Correction 出口迁到 WA7-R Digest/稀疏 Scope Correction，随后删除完整成员 DAG/Boundary 消费；9780 的 Target step 886 长窗口缺陷仍需在 WA8.5/WA9 前关闭。

## 2026-08-03 WA8 Dirty Mass Apply 恢复点

[COMPUTED][HIGH] `UCrowdDemoMassSubsystem` 现在由 Lifecycle Owner 持久维护 `StableEntityRef→FMassEntityHandle` 索引；Spawn 注册、Recycle 原子移除旧 Lifecycle 并注册新 Lifecycle、Destroy/teardown 清空。解析时同时验证 Handle 有效性和 `FCrowdMassAgentFragment` 的完整 StableRef，禁止只按 AgentId 命中复用实体。

[COMPUTED][HIGH] Worker Proxy 刷新把本批唯一 Dirty StableRef 保留到当前 Step；bootstrap 只在首次/Plan Revision 边界把完整成员标为 Dirty。最终 `ResultCommitQuery` 不再执行无界 `ForEachEntityChunk`，而是先解析全部 Dirty Handle、验证无重复/Lifecycle/Commit Record/Query Fragment 集合，再调用 `ForEachEntityChunkInCollections` 原子写入命中实体。Checkpoint/PostFinalize 记录从已验证 Worker/Boundary 结果构造，不再为生成记录读取完整 Mass Fragment。

[COMPUTED][HIGH] Development Editor DisableUnity、`CrowdDemo.Architecture.PostFinalizeMinimalQuery`、`CrowdDemo.FriendlyLogistics.Architecture` 与 `MassCrowd.Runtime.WorkerResultApply.LifecycleOwnerAndEvents` 通过。9782 T5 到 600 Tick，Guidance=`20/20`、stale=`0`、零 `VIOLATION/Rejected`。9784 T8 到 900 Tick，事件 50/50/50/50/50、duplicate/expired=`0/0`、三 Hash=`439379904/1411313634/6141440`；Dirty Mass 每批为 20/20，因为该高动态场景全部实体持续 Dirty。

[INFERRED][HIGH] WA8 仍 OPEN：Demo-local Round DAG、完整 `BoundarySnapshot`、完整 Commit/Checkpoint CPU 数组仍按 O(N) 构造，且 9780 的 T5 step 886 Target Demand 可行区不足未修复。下一步应先把普通帧 PostFinalize/Checkpoint 数组改为低频 Checkpoint 或 Dirty 增量，并删除完整成员 DAG 消费；随后修复 Target 长窗口缺陷，再进入 WA8.5/WA9。

## 2026-08-03 WA7-R Digest 与 WA8 Dirty Proxy/Gather 删除恢复点

[COMPUTED][HIGH] Authority Digest 已从 `Client, Reliable` 改为 `Client, Unreliable`。`FCrowdWorkerAuthorityDigestInbox` 保留最高已接受序列：允许中间 Digest 丢失、拒绝迟到/重复序列、由更新 Digest 覆盖未消费旧值，并在 resync reset 后重新建立序列基线。Development Editor DisableUnity 与 `MassCrowd.Networking.Replication.WorkerCodec` 通过。

[COMPUTED][HIGH] `FCrowdWorkerResultApplyProxy` 现在维护只在 Lifecycle 变化时重建的稳定实体视图和 Stable Slot，并发布两阶段确认的 Dirty Batch。普通 Intent 不再调用 `CopyCurrentEntities`；正常 Proxy refresh 只遍历 Dirty Record，先完整解码/验证命中 Facing/Combat，再按 Stable Slot 原子更新缓存并 ACK。正常块中的完整实体复制、排序、临时 EntityRef→record TMap、完整 Movement 数组和逐实体 `FindDomain` 已删除。

[COMPUTED][HIGH] `FCrowdDemoRoundBoundaryGatherStage`、`RequestSubmitQuery` 与 Result Apply 内的完整 Mass fallback 已物理删除。唯一完整 Mass Snapshot 读取移到 Worker Input Sync 的首次 bootstrap/Plan Revision 边界，并由 `TryRecordBootstrapMassRead` 和一次性 bootstrap 使用权约束；普通 Tick 只能走 Dirty Batch，缺失时 fail-closed。

[COMPUTED][HIGH] Development Editor DisableUnity、`WorkerCodec`、`WorkerResultApply.LifecycleOwnerAndEvents`、`MassCrowd.Runtime.GatherMergeCommit` 与 `CrowdDemo.Architecture.PostFinalizeMinimalQuery` 通过。9779 T8 完成 900 Tick Golden，三哈希=`439379904/1411313634/6141440`；9781 T5 完成干净 600 Tick，Guidance=`20/20`。两者 step 600 均为 full-publish/hash/token=`1/3/598`、stale lifecycle=`0`，无 `VIOLATION/Rejected`。

[COMPUTED][HIGH] 延长诊断运行 9780 在 T5 step 886 触发 Target Demand 可行区不足拒绝；它不否定本切片固定的 600 Tick 结构门，但属于进入 WA9 前必须单独关闭的 Target 长窗口缺陷，不能被脚本退出码掩盖。

[INFERRED][HIGH] WA8 仍 OPEN：Demo-local Round DAG 与 `BoundarySnapshot` 仍持有完整成员数组，最终 `ResultCommitQuery` 仍执行完整 Mass Commit；下一切片应建立持久 StableEntityRef→Mass Handle/Dirty Apply Plan，让最终 Mass 写入也只触及 Dirty Entity。之后再完成 SoA/Particle Island 等 WA8.5 缺口，不能直接进入 WA9。

## 2026-08-03 WA8 Production Snapshot Hash 降频恢复点

[COMPUTED][HIGH] 全 Worker Production 普通 Tick 已不再遍历 `BoundarySnapshot.Agents` 计算完整 StableHash。`AdvanceBoundarySnapshotEpochToken` 使用可信 bootstrap baseline、绝对 FixedStep、Plan Revision 与 `LastAppliedInputSequence` 生成 O(1) Epoch Token；完整 Hash 只在 bootstrap/fallback publish、每 300 Tick checkpoint、Shadow/Canary 或 `-CrowdDemoFullBoundarySnapshotDiagnostic` 下计算。

[COMPUTED][HIGH] 新增 full snapshot hash、epoch token 与 full publish 分流遥测。9772 全 Production T8 在 step 600 得到 publish/hash/token=`1/3/598`，900 Tick 的 acquire/windup/spawn/impact/damage=`50/50/50/50/50`、三哈希=`439379904/1411313634/6141440`、fixed-step p95=`34.058ms`、realtime=`0.998`。9773 移动目标 T5 在 step 600 同样得到 `1/3/598`，普通 Intent 只有单条 Objective resource 增量，stale/block wait=`0/0`，无 `VIOLATION/Rejected`。

[COMPUTED][HIGH] 9771 首次 T8 在 step 0 暴露既有启动竞态：本 Tick 没有 Target topology work、也未提交 Target 输入时，Production parity 校验仍要求 Target state，产生 `legacy=0 worker=0` 误报。校验现已增加 `TargetTopologySlots` 非空门；没有 Objective 时不伪造 Target state，Objective 出现后仍通过 Revision 增量启用 Worker Target。修复后的 DisableUnity、结构门与 9772 T8 通过。

[INFERRED][HIGH] WA8 仍 OPEN：普通 Proxy 路径仍会复制/排序完整实体引用、逐实体查找并解码 Movement/Facing/Combat，Demo-local Round DAG 也仍持有完整成员数组。下一步应让 Proxy 暴露稳定 dense view 与 Published Dirty Batch，让普通 Tick 只验证水位和 Dirty slot；完整成员审计保留在 bootstrap/checkpoint/诊断边界。20 实体 T5/T8 不等于 WA8.5/WA9 的 10k 规模通过。

## 2026-08-03 WA8 普通帧 Snapshot 原位刷新恢复点

[COMPUTED][HIGH] Worker Proxy 普通 Tick 已删除 `WorkerInputSnapshotCache` 及 Formation/Facing/Business 三套重复缓存，不再复制完整 `FCrowdMassBoundaryAgentRecord` 数组，也不再调用 `BuildBoundarySnapshot` 或 `PublishBoundarySnapshot`。bootstrap/fallback 仍允许一次完整 Mass Gather 与 Snapshot Publish。

[COMPUTED][HIGH] 新路径先完整验证 Proxy 当前实体集合，解码全部 Movement/Facing 与必要 Combat hot state；验证完成后才在 Owner Barrier 更新稳定存储。公共 `FCrowdMassRuntimeBridge::RefreshBoundarySnapshot` 对已经稳定排序的数组原位校验和重算 Hash，不复制、不排序，并在乱序、重复或非法字段时 fail-closed。

[COMPUTED][HIGH] Development Editor DisableUnity、`MassCrowd.Runtime.GatherMergeCommit` 和 `CrowdDemo.Architecture.PostFinalizeMinimalQuery` 通过。9766 T8/900 Tick 保持 50/50/50/50/50、duplicate/expired=`0/0`、三哈希一致、p95=`34.192ms`、realtime=`0.998`；step 600 遥测为 in-place/full-publish=`600/1`。9770 T5 到 step 600，普通 Intent resources=`1`，in-place/full-publish=`600/1`，stale/block wait=`0/0`，零 `VIOLATION/Rejected`。

[INFERRED][HIGH] WA8 仍 OPEN：普通 Tick 虽不再完整复制/重建 Snapshot，Demo-local Round DAG 仍持有完整 `BoundarySnapshot` 并每 Tick 计算 O(N) StableHash。下一步应让 Production Business/验证直接消费 Worker Proxy dirty view，将完整 Snapshot/Hash 限制到 bootstrap、Shadow/Canary、Checkpoint 与显式诊断边界；随后才能进入 WA8.5。

## 2026-08-03 WA8 公共 Legacy Orchestrator/WorkGraph 物理删除恢复点

[COMPUTED][HIGH] Round 已解除对插件公共 `MassCrowdBoundaryOrchestrator` 与 `MassCrowdBoundaryWorkGraph` 的最后生产依赖。任务 DAG 只保留 Demo-local `FCrowdDemoRoundWorkBatch`；SharedFlow→Movement→Particle→Facing 的不可变 join 改为 Demo-local `FCrowdDemoRoundWorkGraph`，纯计算 Kernel 继续保留。

[COMPUTED][HIGH] 插件中的 Orchestrator/WorkGraph 公共头、实现与两组 Legacy 专项测试共六个文件已物理删除。结构门新增文件不存在、生产 include 为零和旧 Runtime WorkGraph 符号为零的审计；删除后的 Development Editor DisableUnity 与 `CrowdDemo.Architecture.PostFinalizeMinimalQuery` 通过。

[COMPUTED][HIGH] 9765 重跑全 Production T8/900 Tick 通过：acquire/windup/spawn/impact/damage=`50/50/50/50/50`，duplicate/expired=`0/0`，三哈希=`439379904/1411313634/6141440`，fixed-step p95=`34.230ms`、realtime=`0.998`。这证明公共外壳删除没有改变异步 DAG、原子 Apply 或 Projectile→Combat 事实流。

[INFERRED][HIGH] WA8 仍不能整体关闭：Round 仍保留名为 Boundary Transaction 的 Demo-local 批次状态词汇，且正常帧 `BoundarySnapshot`/Mass Gather 是否已降为 bootstrap-only 尚未完成符号与实际 Query 注册审计。下一步先完成这两项并证明普通 Production Tick 不构造完整 Snapshot，再进入 WA8.5；不能用 20 实体 T8 代替 10k 复杂度门。

## 2026-08-03 WA8 Round 四阶段外壳删除恢复点

[COMPUTED][HIGH] `ExecuteAuthorityInputStage`、`ExecuteResultCommitStage`、`ExecutePostCommitStage`、`ExecuteRequestSubmitStage` 与 `PollRoundWorkBatch` 已从生产源码物理删除。Worker Input Sync 直接执行 Plan Apply；Worker Result Apply 先消费 Published Batch，再由单一 `AdvanceRoundWorkerFrame` 完成上一批 Apply 准备、完整 Commit、Authority/Client 发布、PostFinalize/Checkpoint 和下一批提交。原子验证、失败零部分写入及两个边界各自独立的 Movement/Particle/Facing Stage 生命周期保持不变。

[COMPUTED][HIGH] `CrowdDemo.Architecture.PostFinalizeMinimalQuery` 已反转旧断言：现在逐符号拒绝四个 Legacy stage 和 Poll shell，验证 `TryPrepareRoundApply` 明确准备门、Input Sync 直接 Plan Apply、Result Apply 单一 advance，以及 Processor header 中恰有两个 `UMassProcessor` 类型。Development Editor DisableUnity、结构门与 `git diff --check` 通过。

[COMPUTED][HIGH] 9757 的 T5 移动目标全 Production 再次达到 step 600：正常 task=`5`、stale=`0`、ordinary block wait=`0`、simulation lag=`0`，无 `VIOLATION/Rejected` 或 Particle fixture。该结果验证合并后的 Pending→Commit→PostFinalize→下一批时序在 Target/Particle 路径闭合。

[COMPUTED][HIGH] T8 黄金门已由 9764 关闭。第一层根因是 Projectile `ImpactId=ProjectileId` 使用 source-major 标识，而 Combat 的单值 `LastConsumedHitEventId` 要求事件随 Tick 单调；现改为 Tick-major 高 32 位与 Projectile 稳定散列低 32 位，并对同 Tick 碰撞 fail-closed。第二层根因是 Movement Profile 拆分丢失了组合 Guidance 的所有权：Legacy 已选择 `BusinessOverride=Stop`，Worker Planning 却重新选择 Shared Flow/Behavior，导致 step 0 多移动 26.667cm、速度误差 800cm/s。Movement Profile v2 / MovementControl v9 新增 `bUseAuthoritativePreferredVelocity`，显式阻止重新选源，同时保留 Behavior lock、Combat death/reactive/lock 与速度约束。

[COMPUTED][HIGH] Development Editor DisableUnity、14 项 `CrowdDemo.Combat.T8`、`MovementControlRoundTrip`、`MovementPlanningParity` 均通过。9764 全 Production 900 Tick 得到 acquire/windup/spawn/impact/damage=`50/50/50/50/50`、duplicate/expired=`0/0`，三哈希恢复为 `439379904/1411313634/6141440`，fixed-step p95=`34.181ms`、realtime=`0.998`。临时逐步 Projectile 诊断日志已删除。

[COMPUTED][HIGH] 后续切片已迁出 WorkGraph/Orchestrator 的最后生产消费者并物理删除公共 API；本段保留为四阶段外壳删除时的历史恢复点。20 实体 T8 仍不能冒充 WA8.5/WA9 10k 规模门。

## 2026-08-03 WA8 Target Objective → Particle 动态闭环恢复点

[COMPUTED][HIGH] Target 场景的移动目标现在只有一个逐 Tick `ObjectiveRevision` 输入。Worker Target 与 Worker Particle 消费同一版本化 Objective；Particle 中保留的外部目标模板只提供半径、间隔和 Mobility，`StartPosition/PredictedPosition` 每 Tick 由 Objective 的位置、速度与固定步长重建。正常帧不再从冻结的 bootstrap `MovementControl` 读取目标运动学。

[COMPUTED][HIGH] 新增 `MassCrowd.RuntimeV2.ParticleUsesLiveTargetObjective` 行为回归：MovementControl 模板故意位于 10000cm 外，Objective 把目标移动到 50cm 并赋予速度，Worker Particle 必须产生非零纠偏，同时声明并观察 Objective Resource 依赖。Development Editor DisableUnity、该回归、Particle 6/6、GatherMergeCommit、TargetControlRoundTripAndDomain 与 PostFinalizeMinimalQuery 均通过。

[COMPUTED][HIGH] Particle 最终安全闭包已由四个真实失败窗口的 21-agent 精确 fixture 锁定。终态 exact-safe 时，`UnifiedHardInfeasibleCount` 只保留为诊断，不再强迫 Runtime 回退到已知不安全的起点；量化 swept closure 先走公共 progress，再走稳定旋转候选与每序最多 1000 节点的有界搜索，禁止恢复无上限 CSP。四 fixture 回归约 0.2 秒完成。

[COMPUTED][HIGH] 9756 的 T5 移动目标全 Production 运行达到 step 600：普通 step 300/600 的 Round task 数均为 `5`，Runtime failure=`0`、simulation lag=`0`、stale lifecycle=`0`，日志无 `VIOLATION/Rejected`，也没有生成 Particle failure fixture。普通 Intent 在 step 300/600 均只有 `resources=1`，对应 O(1) Objective 增量，没有完整 Target cohort 或实体状态输入。

[INFERRED][HIGH] 该结果关闭 20 实体 T5 的 Target Objective→Particle 输入合同缺陷，但不关闭 WA8 或 WA9。下一结构切片应物理删除 Round 的四阶段/Poll shell，并把注册审计改为符号/AST 证据；随后才进入 WA8.5 的 10k 复杂度门。

## 2026-08-02 WA8 Round Runner 物理删除恢复点

[COMPUTED][HIGH] Round 已不再包含 `MassCrowdBoundaryRunner.h`、`FCrowdMassBoundaryRunner`、`FCrowdBoundaryTransactionId`、`BuildAndSealCommit` 或 `FCrowdBoundaryCommitEnvelope`。Round 使用 Demo 本地 `FCrowdDemoRoundWorkBatch` 调度不可变 Kernel 输入，完成后由 `ValidateRoundApplyPlan` 直接验证完整 Commit Target/Lifecycle 与各业务摘要哈希，再进入唯一原子 Mass Apply。

[COMPUTED][HIGH] 插件公共 `MassCrowdBoundaryRunner.h/.cpp` 及其两项 Legacy Runner 测试已物理删除。零消费者审计确认 Runner 删除前除自身和测试外没有生产消费者。公共 Orchestrator 类型与 WorkGraph 暂不删除，因为 Round 仍消费任务/遥测类型和纯输入组装 helper；这两项仍是 WA8 OPEN。

[COMPUTED][HIGH] Development Editor DisableUnity 与 `CrowdDemo.Architecture.PostFinalizeMinimalQuery` 已通过。9724 的真实全 Production T8 通过 step 300：tasks=`5`、pending=`301`、stale=`0`、ordinary block wait=`0`，Worker Projectile 同步到 step 300。

[COMPUTED][HIGH] 9720/9722 暴露并复现一条旧 Shadow 断言错误：`ResolvedHits.Hits.Num()` 被错误比较到 `AppliedHitCount`；目标生命周期无效时合法值为 input=`1`、applied=`0`。现已改为比较 `InputHitCount`，同时保留 HitSummary 全字段/StableHash 的严格 Worker-Legacy 比较。

[COMPUTED][HIGH] 9724 在 step 415 因 `CrowdDemoParticleFailureTraceReplayMismatch` 终止，候选哈希=`1192703021/3195638420`。因此本切片只关闭 Runner 删除与 300 Tick Round 生命周期门，不关闭完整 T8；下一步先修复 Particle failure-trace replay 的确定性输入合同，再跑完整 900 Tick T8，之后才删除 Round 四阶段/Poll shell。

## 2026-08-02 WA8 Demo Runner/WorkGraph 脱钩恢复点

[COMPUTED][HIGH] Friendly 与 Mixed Coordinator 已不再引用 `FCrowdMassBoundaryRunner`；Mixed 也不再引用 `FCrowdMassBoundaryWorkGraph`。Friendly Shadow/Canary 直接调用纯 Nav/Facing 计划并继续走原子 Mass Apply；Mixed fallback 在线程池中按 Movement → Particle → Facing 执行纯 Kernel 链，GT 只在 Future ready 后完整解析 Slot/Lifecycle/Transform，再一次性提交。旧 BuildAndSeal/MarkValidated/MarkCommitted transaction envelope 已从这两个 Demo 入口删除。

[COMPUTED][HIGH] Development Editor DisableUnity 构建、`CrowdDemo.MixedSandbox.J.Architecture` 与 `CrowdDemo.FriendlyLogistics.Architecture` 均通过。9706 验证 Behavior Production + Movement fallback 的新异步链在 600 Tick 完成 Mixed 闭环，fixed-step p95=`17.848ms`；9707 验证 Friendly Movement/Behavior Production 完成 5 次交付及全部业务异常分支；9709 验证 Mixed 六 Domain Production 在双端完成 600 Tick，服务端 fixed-step p95=`18.191ms`、stale lifecycle=`0`、simulation lag=`0`。

[COMPUTED][HIGH] 9709 的客户端 frame p95=`35.949ms`，高于 WA9 固定门 `33.333ms`，因此该次只关闭功能回归，不关闭 WA9 性能门。9708 的 Mixed Combat server 实际完成 combat PASS，但脚本同时要求普通 MixedSandbox PASS，故该次命令退出失败，不能记作组合门通过。

[COMPUTED][HIGH] 9705 的 step 2 失败已修正为明确的预测期合同：bootstrap 继续严格比较完整 kinematics/source state；普通 Intent 只要求 Source binding、实例控制载荷/时间线、controller cursor 与命令 hash 一致，不再把 Worker 自主预测 Position/Velocity 驱动的 resolved/evaluator state 差异误判为输入协议错误。新增 `MassCrowd.BehaviorSource.CommittedPredictedKinematics` 单元门通过。

[COMPUTED][HIGH] 9710 进一步在 expectation 63 锁定真实自主状态分叉：相同 target context 下，GT/Worker 位置已不同，`MaintainDistance(11006)` 分别进入 Approach/Hold；控制载荷、timeline 与 cursor 完全相等。9714 使用最终合同完成 Shadow 600/600 expectation，双端 Mixed PASS，server/client p95=`17.748/12.224ms`，零 violation。

[INFERRED][HIGH] 当前正确下一步是迁移 Round 对 Runner/WorkGraph 的最后一个生产依赖。现有生产消费者审计显示 Demo 中只剩 `CrowdDemoRoundSimPipelineSubsystem` 引用两类公共 API；插件 Runtime 实现与 Legacy 专项测试仍存在。Round 迁移和定向回归完成前不得物理删除公共类型。

## 2026-08-02 WA8 Friendly Production 直接 Worker Apply 恢复点

[COMPUTED][HIGH] Friendly 在 Movement 与 Behavior 同时为 Production 时不再创建或执行 `FCrowdMassBoundaryRunner`：bootstrap 一次发布 `Entries=0` 的 MovementControl v8 和 20 条静态 `MovementProfileRevision`，普通 Tick 仅提交 Clock/Behavior/Lifecycle 增量；GT 等待同一 InputSequence 的 Behavior 与 Facing tail，完整验证后构造 Commit Plan 并调用 Mass 原子提交。Shadow/Canary 仍保留 Nav/Runner 对照分支。

[COMPUTED][HIGH] 9703 真实双端门通过：服务端 `direct_worker_apply=1`，20 实体完成竞争、取货、死亡复用、fallback、两次不可达退避、取消和交付；source/sink/in-transit=`35/5/0`，客户端 cargo attach/detach=`2/2`，双端 state hash=`3180435972084878253`，硬失败为 0。step 300/600 普通输入的 resources/spawn/despawn/profile 均为 0；step 606 生命周期复用仅发送 spawn/despawn/profile=`1/1/1`，`resnapshots=1` 未增长。

[COMPUTED][HIGH] Development Editor DisableUnity 与 `CrowdDemo.FriendlyLogistics.Architecture` 通过；运行脚本在同时开启 Movement/Behavior Production 时会强制要求 `direct_worker_apply=1`，旧 Runner PASS 文本不能再伪装成直接 Apply 证据。

[INFERRED][HIGH] 下一切片固定为移除 Mixed/Friendly Shadow/Canary 对旧 Runner/WorkGraph transaction shell 的依赖，同时保留 kernel parity；随后删除无消费者的公共 Runner/WorkGraph API 与 Legacy 测试，再进入 Round 四阶段/Poll shell 删除。Mixed TargetControl 完成前继续保持 LocalPredictive 关闭。

## 2026-08-02 WA8 Mixed Production 直接 Worker Apply 恢复点

[COMPUTED][HIGH] full-Worker Mixed Production 现在在输入提交后直接等待同一 `WorkerBehaviorInputSequence` 的 Published Batch 闭合，从 Worker Facing dirty proxy 构造 Commit Plan，完整解析 `StableEntityRef -> MassEntityHandle/FTransformFragment` 后一次性写入；该分支不再创建或执行 `FCrowdMassBoundaryRunner`/WorkGraph。Shadow/Canary 与非全 Production 组合仍保留 Legacy 对照路径。

[COMPUTED][HIGH] MovementControl codec 已升为 v8：bootstrap 只发布一个 `Entries=0` 的全局设置资源，并用逐实体 `MovementProfileRevision` 建立静态 profile；普通 Tick 不再复制完整 `MovementControl.Entries`。导航 InteractionLayer 变化会在原子提交成功后进入稀疏 profile journal；安全索引、Slot nav state 与 Mass Transform 均先暂存，完整验证后再共同提交。

[COMPUTED][HIGH] 9699 诊断证明首个直接 Apply 阻塞不是 InteractionLayer：Worker Behavior 对实体 1 输出非零 `DesiredVelocity`、`goal=1`、`locked=0`，但 Target/可达 Flow 均不存在，MovementPlanning 旧分支因存在 Goal 而拒绝回退 Behavior velocity，最终 velocity 为零。该分支已修复，并由 `MassCrowd.RuntimeV2.MovementPlanningParity` 的“有 Goal、无 Target/Flow”覆盖锁定。

[COMPUTED][HIGH] Mixed 当前未发布 TargetControl cohort/flow，因此 sparse bootstrap 显式关闭 LocalPredictive，保留 Worker Movement、Particle non-overlap 与 GT 原子安全门；否则直接目标向量在高密度交战中形成永久 all-agent yield。9701 full-Worker Mixed step 600 PASS：alive=`7`，intent=`169`（melee/mid/ranged=`99/27/43`），impact/damage/death=`66/66/13`，target switch=`189`，projectile spawned/impacted/expired/active=`43/13/27/3`，duplicate/stale=`0/0`，min separation=`70.04cm`，p95=`17.641ms`，全日志零 `VIOLATION/Rejected`。

[COMPUTED][HIGH] Development Editor DisableUnity、MovementControlRoundTrip、MovementPlanningParity 与 Mixed Architecture 定向门通过。WA8 仍 OPEN：Friendly Production Runner、Round 四阶段/Poll/Boundary shell、Mixed 的 fallback/public Legacy API 尚未物理删除；Mixed TargetControl 合同也尚未完成，不能重新开启其 LocalPredictive。

[INFERRED][HIGH] 下一切片固定为 Friendly Production 直接 Worker Apply；完成真实 Friendly 门后，再删除无生产消费者的公共 Runner/WorkGraph 与 Round 四阶段 shell。不要在 TargetControl 完成前把 Mixed LocalPredictive 改回开启。

## 2026-08-02 WA8 full-Worker Mixed 600 Tick 所有权门关闭

[COMPUTED][HIGH] 9688 首次 full-Worker Mixed Production 在 step 6 被 `worker_projectile_parity` 拒绝；唯一摘要分歧为 Legacy/Worker `TargetSwitchCount=1/0`。该拒绝阻止 GT 提交已经验证的 Worker SourceSet，下一 Tick 因此向已推进到 sequence 5 的 Worker 重发 stale sequence 4，继发 Behavior `DomainExecution`。这不是独立的 Behavior command 生成错误。

[COMPUTED][HIGH] Production apply 现在只以 Worker projectile/combat 的 control revision、absolute fixed step、payload/schema/hash、entity/lifecycle membership 与 alive/health 自洽为提交门；Legacy projectile hash、host result 和逐实体 combat payload parity 只保留在 Shadow/Canary。Production 的 attack/projectile 累计指标改为读取 Worker `Prepared.Summary`、`ResolvedHits` 和 decoded host result。

[COMPUTED][HIGH] 9689 full-Worker Mixed 在 Movement/Behavior/Target/Particle/Projectile/Combat 全 Production 下于 step 600 PASS：population/alive=`20/18`，attack intent=`82`（melee/mid/ranged=`36/3/43`），impact/damage/death=`21/20/2`，target switch=`79`，projectile spawned/impacted/expired/active=`43/18/23/2`，duplicate=`0`，conserved=`1`，stale reject=`0`，fixed-step p95=`17.624ms`。验证继续到 step 1055，日志仍无 `VIOLATION`、`Rejected` 或 parity 拒绝。

[COMPUTED][HIGH] Development Editor DisableUnity 与 `CrowdDemo.MixedSandbox.J.Architecture` 通过。WA8 仍 OPEN：Production GT 仍预计算 Legacy combat/projectile 期望事务；Friendly/Mixed Legacy Runner、Round 四阶段事务外壳和 Boundary shell 尚未物理删除。

[INFERRED][HIGH] 下一切片固定为删除 Mixed Production 的 Legacy combat/projectile 预计算与期望载荷，不改变 Shadow/Canary 对照；随后重跑 full-Worker Mixed 600 Tick，再继续 Friendly/Mixed Runner 与 Round shell 删除。

[COMPUTED][HIGH] 上述下一切片已由 9690 完成：Production 不再调用 `PrepareMixedCombatBoundary`，因此不再执行重复 Projectile pipeline、Impact resolve、Health patch 或 expected host-result encode；Shadow/Canary 保留完整 parity。9690 step 600 的业务与 entity/membership/commit hash 和 9689 相同，p95=`17.821ms`，stale reject=`0`，全日志零 `VIOLATION/Rejected`。GT Attack Planner 暂留，因为当前 Mixed Business Planner 仍用当 Tick Attack Phase 生成 MovementLock/行为标签。

[INFERRED][HIGH] 当前下一项改为迁移 Mixed Business Planner 的 Attack Phase 消费，使 Worker Combat 状态在 Behavior planning barrier 可见；完成后才能删除 GT `PrepareMixedCombatAttackPlan`，随后才是 Friendly/Mixed Runner 与 Round shell 的物理删除。

[COMPUTED][HIGH] 现有 DAG 审计修正了上一句的实现方向：`MovementPlanning` 已显式依赖 `CombatReactive` 并在同一 Epoch 读取通用 Combat state，因此 Attack Commit 的正确消费者是 Worker MovementPlanning，不应把 Demo AttackState codec 耦合进通用 Behavior planner。通用 Combat codec 已升级为 v2 并新增 `bMovementLocked`；Demo Combat extension 在存活实体 Commit Phase 设置该位，MovementPlanning 以 dead → reactive → attack lock 的优先级消费，并忽略 Combat 实体上重复的 Behavior lock。

[COMPUTED][HIGH] Mixed Production 现已跳过 GT `PrepareMixedCombatAttackPlan`；Shadow/Canary 保留旧 planner/parity。9691 full-Worker Mixed step 600 PASS：intent=`82`，impact/damage/death=`21/20/2`，target switch=`79`，projectile=`43/18/23/2`，duplicate=`0`，stale reject=`0`，p95=`17.880ms`。Combat domain、MovementPlanningParity、Mixed Architecture 与 Development Editor DisableUnity 均通过；全日志零 `VIOLATION/Rejected`。

[INFERRED][HIGH] WA8 下一项现为拆除 Mixed/Friendly 的 `FCrowdMassBoundaryRunner`/WorkGraph Legacy transaction shell，并让 Published Batch validation → Dirty Apply Plan → Mass Proxy commit 成为唯一 Production apply 路径。

## 2026-08-02 WA8 最新恢复点：T8 串行帧屏障已关闭

[COMPUTED][HIGH] 9686 先把 Production Movement/Particle/Facing tail 改为从已验证 Worker 输出同步构造原子 Commit Plan，移除第二次异步 tail 等待；两轮 fixed-step p95=`50.944/51.017ms`、realtime=`0.668/0.669`、boundary pending=`1800/1798`，业务闭合不变。这证明每 Tick 三次帧级等待已减少为两次。

[COMPUTED][HIGH] 9687 随后把同 Generation、同 Plan、资源已 bootstrap、无 Target Region 且 Movement/Behavior/Particle/Projectile/Combat 全 Production 的普通 T8 Clock Intent 提前到 Legacy domain staging 之前；首次/换轮 baseline 和其他模式仍走完整 prepared 路径。两轮 fixed-step p95=`33.999/33.981ms`、realtime=`0.998/1.000`、boundary pending=`902/901`，`VIOLATION/Rejected=0`。

[COMPUTED][HIGH] 9687 两轮均完成 900 Tick；Round 1 acquire/windup/spawn/impact/damage=`50/50/50/50/50`，attack/projectile/event hash=`439379904/1411313634/6141440`，Round 2 同样为50次闭合。Round 2 在 Generation=`1`、PlanRevision=`2` 上重新提交 baseline 后从 step 1 恢复提前 Clock Intent。

[COMPUTED][HIGH] Development Editor DisableUnity 与 `CrowdDemo.Architecture.PostFinalizeMinimalQuery` 通过。WA8 仍 OPEN：下一项是全 Worker Mixed 600 Tick 完整业务门；Friendly/Mixed Legacy GT Runner、Round 四阶段事务外壳和 Legacy shell 物理删除仍未完成。

## 2026-08-02 WA8 Round transition 历史恢复点

[COMPUTED][HIGH] 9685 全 Worker T8 在同一 Runtime Generation、`resnapshots=1` 下完成 Round 1 900 Tick，并让 Round 2 从 step 0 继续到 step 300；没有 Runtime restart、Checkpoint resync 或 Domain execution rejection。9680 的 Round 2 continuation 阻塞已被替代。

[COMPUTED][HIGH] 本切片修复 Projectile fresh Revision reset、MovementControl Resource 对 anchored TimeWheel continuation 的覆盖，以及 PlanRevision 变化时的一次性 InputSnapshot baseline。Development Editor DisableUnity 与 `ProjectileCombatDomain`、`MovementPlanningParity`、`AutonomousNoEventCapture`、`PostFinalizeMinimalQuery` 定向门通过。

[COMPUTED][HIGH] 9685 当时 WA8 仍 OPEN：Round 1 fixed-step p95=`67.854ms`、realtime=`0.500`、boundary pending frames=`2698`。该性能缺口已由上方9686/9687取代；全 Worker Mixed 完整业务门与 Legacy shell 删除仍未完成。

## 当前分支与事实源

[COMPUTED][HIGH] 工程位于`E:\Projects\SuperInvincibleTank_MASSAITEST`，当前分支为`codex/t5-target-region-transport`。

[COMPUTED][HIGH] 当前架构事实依次以`CurrentArchitecture.md`、`EntityBehaviorSourceArchitecture.md`、`MassProjectileHitFrameworkDesign.md`为准；阶段与验收分别以`PhasePlan.md`、`FeatureChecklist.md`和`TestScenarioMatrix.md`为准。

[COMPUTED][HIGH] PJ0之前的逐轮恢复记录已迁入`Docs/History/RecoverySnapshotsBeforePJ0_20260729.md`，不得从历史快照恢复已经被后续阶段替代的结论。

## 当前完成状态

[COMPUTED][HIGH] 2026-07-30最新架构决策为WA0–WA9全面Worker权威，已取代“四个GT Boundary Processor”作为AB5终态。现行目标事实源是`FullWorkerAuthorityArchitecture.md`，字段Writer事实源是`FullWorkerAuthorityOwnershipMatrix.md`；`AB5FourNodeBoundaryContract.md`只保留历史。

[COMPUTED][HIGH] WA0–WA7-R已完成。WA7-R已关闭有序Intent、Digest、稀疏Correction、Checkpoint、Late Join顺序门及300 Tick双PIE无纠错/单Scope恢复门。WA8进行中：Round、Friendly与Mixed普通Worker输入均已迁到Intent入口，但Friendly/Mixed Legacy GT业务Runner、Target/Projectile剩余输入和Round四阶段事务外壳仍在，不得当作全面Worker终态验收。

[COMPUTED][HIGH] R0–R7开放Behavior Source、通用Boundary Scheduler、Behavior网络v3、Mass持久权威Projectile和可选StateTree拆分已经完成。

[COMPUTED][HIGH] S0–S6 Standard Sources已经完成：插件提供13种通用Source，Demo按五个ControllerId维护稳定Source集合Diff，Movement/Facing/Constraint消费Resolved Channels。

[COMPUTED][HIGH] PJ6最终自动化记录为`MassCrowd 65/65`和`CrowdDemo 125/125`；Development/DebugGame的`-ForceUnity -DisableAdaptiveUnity`与`-DisableUnity`四构建均通过。

[COMPUTED][HIGH] 同一Mixed Source/Resolver/Boundary/Networking/Projectiles路径的20/100/500双端门均通过，并分别并发4/20/100发Projectile。服务端p95=`2.152/9.675/30.016ms`，客户端p95=`4.963/4.938/5.171ms`，最小同层间距=`70.11/70.03/70.00cm`；spawn/impact/damage守恒、duplicate=`0`、双端实体/成员/Projectile Hash一致、resync=`0`且零安全违规。

## 当前实施阶段

[COMPUTED][HIGH] PJ0–PJ6已经全部完成；当前没有未关闭的Projectile模块化阶段。

[COMPUTED][HIGH] DP0–DP6 Demo业务规划模块化已经关闭；独立pre-T9恢复基线是已推送提交`5b947389`。T9 Mixed Combat Integration也已关闭，本节及上方事实源共同构成post-T9恢复状态；不得再从pre-T9章节恢复“尚未开始”的结论。

[COMPUTED][HIGH] T9生产合同为20实体10对10、无重生、每方4 Melee/2 MidRange/4 Ranged；统一五阶段攻击Planner输出Attack Intent，Melee/MidRange走Spatial Sweep，Ranged走Mass Projectile，三类Impact统一进入Combat Resolver和Prepared Health/Death提交。

[COMPUTED][HIGH] 8517双端门通过：alive=`11`、三类intent=`61/31/21`、impact/damage/death=`62/61/9`、target switch/TargetRegion rebuild=`227/227`、referenced dead=`0`、Projectile守恒且duplicate=`0`、双端Hash一致、p95=`2.910ms`。

[COMPUTED][HIGH] T9最终自动化为MassCrowd `64/64`、CrowdDemo `133/133`；T8双端版本化黄金门与Development/DebugGame × ForceUnity/DisableUnity四构建均通过。

[COMPUTED][HIGH] 项目侧`MassCrowdDemoBusiness`已经拥有Demo业务Planner与Prepared合同：Mixed、Friendly和Round T7/T8使用Planner，Round T1–T6与Continuous显式NoBusiness；旧Planner双路径已删除。

[COMPUTED][HIGH] DP6最终证据为MassCrowd 64/64、CrowdDemo 131/131及Development/DebugGame × ForceUnity/DisableUnity四构建通过。Mixed 20/100/500沿用8311/8314/8315同路径结果；Friendly=8303、Continuous=8350、NavFlow=8351、T1–T4=8353–8356、T5=8357/8363、T6=8359–8361、T7=8349，T8黄金门=8365。

[COMPUTED][HIGH] T8在真实StableEntityRef与Demo AgentId分离后保持spawn/impact/damage=50/50/50、duplicate=0和双端一致；canonical SimulationTick Combat时钟后的版本化attack/projectile/event Hash为439379904/1411313634/6141440。旧41852579/488896174/4204062592属于wall-time Combat时钟合同，更早的2730049702/4215166500与3512277419属于旧身份/状态布局。

[COMPUTED][HIGH] 删除`AttackTarget` Source后，现行生产Behavior Registry/Context Schema黄金值为11335697795273479593/7449648488286461483；定向自动化与`RunCrowdDemo.ps1 -RangedProjectileGolden`分别锁定Registry/Schema和T8功能/Hash，双端同时漂移不能通过验收。

[COMPUTED][HIGH] `MassCrowdSpatial`拥有Movement安全索引、稳定Body/Grid和相对/环境Sweep；`MassCrowdCombat`拥有Impact/Hit、Effect Profile、纯Resolver和Prepared Host Commit；`MassCrowdProjectiles`拥有Spawn/Profile/State、Mass Fragment/Trait/Store、Kernel和Boundary Pipeline。Runtime只依赖Spatial且不反向依赖Projectiles，公共模块不引用Demo。

[COMPUTED][HIGH] Projectile持久状态只位于公共Mass Fragment；每个Boundary允许不可变Gather和临时Prepared State，完整验证后一次Final Apply。旧Demo Kernel/Fragment/Store、Round重复入口和运行时双路径均已删除。

## 明确范围外

[INFERRED][HIGH] 动画Root Motion、真实StateTree业务Task、T10玩家完整游戏循环、动态NavMesh topology和原工程迁移不属于T9。

[INFERRED][HIGH] 下一未开始阶段为T10；进入前必须从当前post-T9提交恢复并重新确认玩家命令、技能形状、持续生成和完整游戏循环的范围。

[INFERRED][HIGH] 未经用户新的明确要求，不stage、commit或push。

[COMPUTED][HIGH] 2026-07-30新增AB0–AB6异步Fixed-Step Boundary阶段。AB0–AB4已完成；AB5已切换为AuthorityInput、ResultCommit、PostCommit、RequestSubmit四个直接注册的`UMassProcessor`，旧单顶层 Processor、UObject Stage Adapter、手工`CallExecute()`和阻塞Wait已删除。通用Frame Transaction与1/0/1 Mass访问合同已下沉`MassCrowdRuntime`；Base/Target/Combat四种authority template、Round/Replication Optional Combat Query与确定性默认事实已实现。最新四构建、MassCrowd 85/85、CrowdDemo 139/139及capability结构3/3通过；Base+Target T5S 9321/9322功能、复制、Correction和双端hash通过，但backlog p95=`170.807/136.398ms`高于`66.667ms`，因此AB5仍因该性能回退和完整真实场景矩阵保持开放。AB6尚缺强制Pending Correction、teardown/地图切换、完成顺序、全场景与FFmpeg门。

[COMPUTED][HIGH] 最新Correction顺序为authority apply→Generation失效→Mailbox Poll；8827 T5S普通双端Correction流通过双端input/flow hash一致且无VIOLATION，fixed-step/backlog p95=`17.639/32.070ms`。仍缺“人为延长Worker并在Pending帧注入Correction”的强制stale专项，因此AB6 Correction门不标记关闭。

[INFERRED][HIGH] 目标合同为每World深度1 Mailbox：GT Processor应用Plan/Correction、非阻塞消费Result、完整验证/原子Commit、Post-Commit发布并Gather/Submit下一Request；UE::Tasks Worker只消费不可变POD/SoA并发布完整Result。普通Tick禁止`Wait/Get/Event.Wait`，Server/Client各自拥有Mailbox但共享Worker Pool。

[INFERRED][HIGH] 2026-07-30新增PW0–PW8目标阶段，详细设计见`PersistentWorkerSimulationArchitecture.md`。PW0仅完成文档冻结，生产仍是AB深度1Boundary；目标为每World持久Worker权威SoA镜像、GT单向输入Adapter、Worker连续调度与可变Dirty Result Published Batch、GT每帧一次交换并完整消费当时冻结结果。Mass Work Processor不得充当常驻线程，跨帧状态由Worker Runtime拥有，纯计算由短生命周期UE Task Shard执行。

[INFERRED][HIGH] PW迁移必须先Shadow Mirror和Hash比较，再开放Presentation/低耦合输出，最后迁移Movement；Particle、Target和Combat在专项证明前继续使用现行强一致Boundary。不得同时启用GT/Mass与Worker对同一字段的生产Writer。

[COMPUTED][HIGH] PW1–PW4已关闭。PW4为每World Runtime增加短Task Shadow Scheduler，将SharedFlow、Facing和独立Business以自包含输入接入生产Round逐批重复计算；结果只诊断、不写Mass。9111 step 300为900/900完成、in-flight=0、mismatch=0，Development/DebugGame `-DisableUnity`、MassCrowd 78/78、CrowdDemo 134/134通过。

[COMPUTED][HIGH] PW5已关闭：Worker Owner发布可变/空Published Batch，GT Result Apply Processor每帧一次交换并只写Presentation/诊断代理，未触碰Movement/Particle/Target/Combat生产字段。9112首批20 Patch零stale应用；Development/DebugGame `-DisableUnity`、MassCrowd 80/80、CrowdDemo 134/134通过。

[COMPUTED][HIGH] PW6已关闭：Movement拥有显式Shadow/Canary/Production Owner门、普通输入Echo拒绝、Runtime历史净化、Correction Revision与Client插值；Production最终Mass代理Writer只消费Runtime重算的Movement尾链。9118 Shadow、9116五实体Canary、9117 Obstacle Production和9119 SoftPressure Production零Violation；Development/DebugGame `-DisableUnity`、MassCrowd 82/82、CrowdDemo 134/134通过。

[COMPUTED][HIGH] PW7已关闭：Particle Interaction Island、Target Cohort和Combat Event Boundary均由公共fail-closed评估器执行显式证据判定。9121真实Round分别因开放Island、网络语义未冻结和缺少Rollback证明返回`KeepBoundary`且无Violation；三类强一致Domain仍留在现有Boundary。

[COMPUTED][HIGH] PW8已关闭：Production Movement只由`PersistentRuntimeAuthority`拥有；跨Round绝对时间与rollback-safe Dynamic Flow Hash已修复。9174/9175/9177/9179的1k/2k/5k/10k均持续到step 300且队列为0，10k接受`3010000`状态、Worker lag=`9.677ms`；9154–9170覆盖T1–T9/Mixed/Friendly/Continuous，最终双PIE验证独立Runtime和teardown，9180录屏为20实体、58事件、0 mismatch、0 freeze。Development/DebugGame `-DisableUnity`、MassCrowd 83/83、CrowdDemo 135/135通过。10k完整强一致Boundary约145ms/step，不得恢复为“整条流水线10k实时”的错误表述。

## 2026-07-30 单主体ISM受击闪色与提交前状态

[COMPUTED][HIGH] 客户端受击表现已从主体/HitFlash双ISM收敛为单主体`CrowdInstances`：per-instance custom data固定为`(Frame, PreviousFrame, HitFlashIntensity)`，项目父材质`M_CrowdDemoBug_Runtime_VAT`读取PICD slot 2并对白色BaseColor与Emissive做实例级混合。旧`CrowdHitFlashInstances`、`MI_CrowdDemoBug_Runtime_HitFlash_VAT`及双份Add/Update/Remove路径已删除。

[COMPUTED][HIGH] 材质验证器已锁定项目父材质、PICD索引2、默认白色、Emissive强度1.0、手动VAT播放、完整Material Attributes/WPO连接和旧HitFlash MI不存在。表现自动化覆盖0/1/10/9999实例及SwapRemove，修复了交换槽位后继承被删除实例custom data的问题。

[COMPUTED][HIGH] 当前验证为Development/DebugGame Editor `-DisableUnity`、MassCrowd 83/83、CrowdDemo 137/137、T8黄金门、Continuous 9203、Friendly 9205和T7录像9208通过。9208逐帧显示Knockback只闪白2个目标、KnockUp只闪白2个并约5帧衰减、Death只闪白4个；实例始终20，无第二套ISM、红色副本、重影或Z-fighting。

[COMPUTED][HIGH] 1k/2k/5k/10k服务端Production吞吐复验分别在step 300接受`301000/602000/1505000/3010000`状态且Input Queue为0；10k本次scan=`1.423ms`、owner pump=`2.737ms`、GT apply=`0.408ms`。

[COMPUTED][HIGH] 单主体ISM切片的最终关闭仍被独立PW问题阻塞：9207双端与9213无客户端Mixed 500均在fixed-step 93稳定触发`ECrowdWorkerShadowSubmitResult::RequiresResnapshot`。首次失败时Shadow为submitted=92、compared=91、pending=1、last submitted input=`50321`，而Runtime last applied input=`49680`；本次表现切片没有越权修改PW Runtime。`FeatureChecklist.md`与`TestScenarioMatrix.md`保持该项未关闭。

[INFERRED][HIGH] 后续若继续关闭该阻塞，应单独修复Async Runtime对已接受但尚未应用输入批次的Sequence Gate进度合同，并重新执行Mixed 20/100/500、双PIE、Correction和完整自动化，不得把延长超时或关闭Worker当作修复。

[RULES I BROKE]：[COMPUTED][HIGH] 无。

## 2026-08-02 WA8 Projectile clock / semantic resource checkpoint

[COMPUTED][HIGH] `CombatClock` 已进入 Runtime：存在 ProjectileControl 时每个 SimulationTick 都排 CombatReactive resource work；executor 在 revision 不变时以绝对 tick continuation，且清除 edge-triggered SpawnRequests。
[COMPUTED][HIGH] Round/Mixed 已有 ProjectileControl semantic hash 与 published/reused telemetry。只有 Movement/Worker Runtime Production 使用 semantic reuse；Shadow/Canary 仍逐 Tick发布完整 HostInput，防止迁移比较读取冻结的动态状态。
[COMPUTED][HIGH] Mixed 校验已分离 StateRevision(Worker epoch) 与 ControlRevision(configuration)，并以 payload FixedStep 等待正确输出。
[COMPUTED][HIGH] 9664 `-CrowdWorkerBehaviorMode=Production -CrowdWorkerCombatMode=Production`、Movement Shadow 的真实 T9 在 step 600 PASS；published=600/reused=0，因此只证明兼容，不证明普通 Projectile 输入增量化。
[COMPUTED][HIGH] 历史step 9阻塞已解除：9680全Worker T8完成900 Tick与50/50/50业务终局；9685又关闭Round 2 continuation。全Worker Mixed与realtime性能仍OPEN，不得报告WA8整体closure。
[COMPUTED][HIGH] 已通过 Development Editor DisableUnity、ProjectileCombatDomain、MixedCombatDomain、PostFinalizeMinimalQuery、Mixed Architecture。自动化启动前两条通用 `Condition failed` 仍存在，但具体测试 Result=成功。
[INFERRED][HIGH] Behavior admission、Combat时钟和Round transition均已完成；下一步执行boundary pending性能收敛，期间保留Shadow兼容资源与Legacy parity。

[COMPUTED][HIGH] 2026-08-02 9680检查点：Behavior admission容量、Combat双时钟与Round尾部重复Tick已修复，900 Tick输出50/50/50且Projectile资源published/reused=`1/899`；当时的Round 2 failure随后由9685关闭。现存阻塞为realtime性能及Mixed全Worker业务覆盖。

[INFERRED][HIGH] 下一步直接压缩boundary pending恢复realtime≥0.95；不要回到已关闭的step9或Round transition诊断，也不要开始Legacy shell删除。

[RULES I BROKE]：[COMPUTED][HIGH] 无。

## 2026-08-02 WA7-R / WA8.5 recovery checkpoint

[COMPUTED][HIGH] The active branch is `codex/t5-target-region-transport`.
[COMPUTED][HIGH] WA7-R now uses ordered intent, periodic digest, sparse authority correction, and checkpoint transport; the production runtime created from a checkpoint is explicitly Production rather than Disabled.
[COMPUTED][HIGH] The bounded reliable intent receive window is 1024 batches; this fixed the observed 256-batch client overflow during the dual-PIE prediction gate.
[COMPUTED][HIGH] Production ordinary epochs do not materialize a complete Mirror. `FullMirrorSerializationCount` only advances at first materialization and the 300-epoch checkpoint cadence; Shadow mode retains full per-frame comparison.
[COMPUTED][HIGH] `CrowdDemo.PersistentWorker.SingleProcessDualPIE` passed after bootstrap plus 300 Worker epochs with authority correction disabled: no runtime restart, no resnapshot, no authority-correction/checkpoint traffic increase, predicted dirty state advances, and at most one checkpoint Mirror materialization occurs in the window.
[COMPUTED][HIGH] Target guidance has a stable 128-entity inner shard boundary inside the shared Core guidance setup. It does not rebuild demand/flow indexes per shard. `MassCrowd.Runtime.TargetRegion.GuidanceShard10k` passed with 10,000 agents, 20,064 topology cells, 10,048 terminal slots, 10,000 desired population, and zero source-attachment failures.
[COMPUTED][HIGH] Latest directed validation: Development Editor DisableUnity build passed; Networking 6/6, RuntimeV2 31/31, Runtime.Worker 21/21, TargetRegion 2/2, and the single-process dual-PIE gate passed.
[COMPUTED][HIGH] WA8 is not closed. The server Round pipeline still owns `ExecuteAuthorityInputStage`, `ExecuteResultCommitStage`, `ExecutePostCommitStage`, `ExecuteRequestSubmitStage`, `PollBoundaryWork`, and per-step complete `MovementControl.Entries`.
[COMPUTED][HIGH] Boundary Runner/WorkGraph public APIs are still production consumers in Friendly Logistics and Mixed Sandbox, so they cannot be deleted before those consumers migrate.
[COMPUTED][HIGH] WA8.5 remains incomplete for particle island ownership, dense SoA hot fields, and the remaining hot-path allocation/serialization telemetry. WA9 real 10k scenarios and complete matrix have not started.

## 2026-08-02 WA7-R correction recovery closure

[COMPUTED][HIGH] The network checkpoint contract now carries the authoritative fixed simulation quantum. Worker checkpoint version is 3 and replication codec version is 2; restore rejects a checkpoint whose quantum differs from the runtime configuration.
[COMPUTED][HIGH] Authority digest publication waits for the current propagation closure. A client with a mismatched digest opens an owner correction barrier before the next Poll and cannot resume prediction until the ordered correction is queued and atomically applied.
[COMPUTED][HIGH] Movement and Facing digest hashes exclude correction-lineage metadata while retaining simulated Position, Velocity, Yaw, StartPosition, and SimulationTime. Movement continuation preserves the highest corrected Movement revision even when Production reads Facing as its kinematic baseline.
[COMPUTED][HIGH] The formal `CrowdDemo.PersistentWorker.SingleProcessDualPIE` gate passed after a post-bootstrap 300-epoch correction-off window. It then corrupted one client Movement state, detected exactly one Domain x 400cm Cell scope, applied one sparse cell correction without checkpoint/resnapshot/restart, and matched the next digest.
[COMPUTED][HIGH] Latest directed validation after cleanup: Development Editor DisableUnity build passed; `MassCrowd.RuntimeV2` 31/31, `MassCrowd.Networking.Replication` 6/6, and `CrowdDemo.PersistentWorker.SingleProcessDualPIE` 1/1 passed.

## 2026-08-02 WA8 input-contract separation slice

[COMPUTED][HIGH] The Round bootstrap and ordinary input APIs are now physically separated. Round bootstrap/resync uses `SubmitBoundarySnapshot`; ordinary Round frames call `SubmitIntentBatch`, whose implementation has no `FCrowdMassBoundarySnapshot` parameter or local snapshot materialization and submits through `Shadow.SubmitAutonomousFrame`. Friendly and Mixed remain Legacy snapshot callers pending their own migration.
[COMPUTED][HIGH] `FCrowdWorkerResultApplyProxy::CopyCurrentEntities` exports lifecycle membership in stable `FCrowdStableEntityRef` order. The ordinary intent path uses that set to refresh Movement and Behavior owner membership and to queue autonomous Behavior expectations without a Mass query.
[COMPUTED][HIGH] The Round pipeline switches to the intent-only API after the first full resnapshot. The structure gate locks the absence of BoundarySnapshot from the ordinary input implementation and the post-bootstrap call site; the WorkerResultApply lifecycle test locks deterministic membership export.
[COMPUTED][HIGH] Directed validation passed: Development Editor DisableUnity build; `MassCrowd.Runtime.WorkerResultApply.LifecycleOwnerAndEvents`; and `CrowdDemo.Architecture.PostFinalizeMinimalQuery`.
[COMPUTED][HIGH] A real 20-entity T6 heterogeneous moving-target Production server run accepted the second ordinary intent (`submitted=2`, `simulation_tick=1`) and later reported Runtime `epoch=84`, `accepted_input=297`, `applied_input=297`, with no hard failure.
[COMPUTED][HIGH] This slice was superseded by the Movement Profile freeze slice below. At this historical point the ordinary intent still carried three resources, including complete per-entity MovementControl.

## 2026-08-02 WA8 Movement Profile freeze slice

[COMPUTED][HIGH] Round now publishes the complete `FCrowdWorkerMovementControlResource::Entries` profile only for bootstrap/resync, a changed Worker Generation, or a changed PlanRevision. Ordinary clock intents neither construct nor encode MovementControl; the explicit telemetry reports `published=1, reused=1` on the first ordinary frame.
[COMPUTED][HIGH] MovementPlanning plan payload schema is version 4. It carries the next blocked-age state and the surviving local-predictive grant component/epoch/remaining steps, so continuation is part of checkpointed entity state rather than executor-local memory. MovementPlanning schedules one resource wakeup for the next absolute SimulationTick and fans out per-entity Movement work after recomputing the plan.
[COMPUTED][HIGH] Movement, MovementPlanning, and Particle use `AbsoluteSimulationTick` rather than the frozen profile's bootstrap FixedStepIndex. Particle activation also observes Worker Combat alive state. Target state consumption continues to respect each static `bUseWorkerTargetGuidance` owner gate; Canary/Shadow entities are not silently promoted to Target Production ownership.
[COMPUTED][HIGH] The directed two-tick `MassCrowd.RuntimeV2.MovementPlanningParity` test advances the second tick by clock/input waterline only, without updating MovementControl, and still emits two plans, two Movement work items, and one next-tick planning continuation. The final `MassCrowd.RuntimeV2` suite passed 31/31; Development Editor DisableUnity and `CrowdDemo.Architecture.PostFinalizeMinimalQuery` also passed.
[COMPUTED][HIGH] Real 20-entity T6 moving-target Production evidence: the second ordinary intent carried two resources rather than the previous three; a longer run reached Runtime `epoch=151`, `accepted_input=349`, `applied_input=349`, and `published_dirty=18117` without resnapshot, planning rejection, or hard failure. A final short run logged Movement Profile `published=1, reused=1, generation=1, plan_revision=1`.
[COMPUTED][HIGH] WA8 remains open. Round still creates a complete Legacy BoundarySnapshot before the intent adapter, same-Plan Spawn/Profile Revision is not yet an independent journal, Target/Projectile inputs are not fully incremental, and the four Legacy stage functions plus `PollBoundaryWork` remain. Friendly and Mixed still consume public Boundary Runner/WorkGraph APIs.

## 2026-08-02 WA8 lifecycle/profile intent slice

[COMPUTED][HIGH] `MovementProfileRevision` is now an ordered external gameplay input and `MovementProfile` is an append-only entity field. Its versioned codec is independent from the frozen global MovementControl resource.
[COMPUTED][HIGH] Shadow and Demo Input Sync accept ordered Despawn, Spawn, and Profile Revision journals on ordinary intent frames. Lifecycle membership is validated as Despawn then Spawn, and the ResultApply/Movement/Behavior owner sets change only after the intent batch is accepted.
[COMPUTED][HIGH] Round bootstrap emits one Profile Revision per entity. MovementPlanning, Movement, and Particle resolve the live entity directory from `EntityStateStore`, prefer per-entity Profile state, and use the frozen MovementControl entry only as a compatibility fallback. Multiple Profile revisions deduplicate to one MovementPlanning resource work item.
[COMPUTED][HIGH] A bootstrap defect exposed by dual PIE was fixed: input-owned Profile records use the same epoch-one baseline as InputSnapshot, so they are valid before the first Worker epoch.
[COMPUTED][HIGH] Sparse Movement correction now atomically invalidates derived Facing, Particle, and MovementPlan state and schedules critical MovementPlanning work. This prevents a corrected Movement record from being overwritten by a stale pre-correction continuation.
[COMPUTED][HIGH] Directed validation passed: Development Editor DisableUnity; `MassCrowd.RuntimeV2` 31/31; `CrowdDemo.Architecture.PostFinalizeMinimalQuery`; and `CrowdDemo.PersistentWorker.SingleProcessDualPIE`. The formal dual-PIE run completed the 300-epoch correction-off window, reported exactly one expected Movement cell mismatch after diagnostic corruption, applied one sparse correction, and matched the next digest without resnapshot.
[COMPUTED][HIGH] WA8 remains open: the adapter contract and bootstrap producer exist, but Round has no real same-Plan Mass Spawn/Despawn/Profile Revision producer yet; the Legacy BoundarySnapshot and four-stage transaction remain in production.

## 2026-08-02 WA8 real Mass lifecycle/profile journal

[COMPUTED][HIGH] `UCrowdDemoMassSubsystem::RecycleTrackedAgent` is now a real same-Plan lifecycle producer. It creates and initializes the replacement entity first, validates journal capacity and encoded Spawn/Profile payloads, then destroys the old entity and appends ordered Despawn -> Spawn -> Movement Profile Revision records to a bounded subsystem-owned journal.
[COMPUTED][HIGH] Worker Input Sync copies the journal without a full Mass query, combines it with the explicit intent inputs, validates candidate membership, submits through the common `SubmitIntentBatch` path, and acknowledges the journal plus changes ResultApply/Movement/Behavior owner membership only after Runtime acceptance.
[COMPUTED][HIGH] Friendly Worker bootstrap uses `SubmitBoundarySnapshot` once; ordinary Worker frames call `SubmitIntentBatch` and may include the prepared Behavior boundary. The public reliable stream publishes the replacement lifecycle Spawn before later business/source records, and stale old-lifecycle source publication is suppressed.
[COMPUTED][HIGH] Client intent backpressure is retryable rather than fatal: rejected-capacity batches remain queued, and each pump submits at most four batches so Result Apply can alternate with catch-up. Autonomous Behavior uses the maximum of bootstrap BoundaryFixedStep and absolute SimulationTick as its current clock ceiling.
[COMPUTED][HIGH] Validation after these changes: Development Editor DisableUnity passed; Networking passed 6/6; RuntimeV2 passed 31/31; the same-batch lifecycle-reuse test passed. Server production run 9626 completed a real recycle with `spawns=1`, `despawns=1`, `journal_profiles=1`, `stale_lifecycle=0`, `mirror_entities=20`, and final `death_recovery=1`.
[COMPUTED][HIGH] External dual-end Friendly is not closed. Run 9625 kept the client Runtime healthy and applying a large intent backlog, but did not emit the client business PASS before the gate window ended. Friendly's Legacy business Runner still constructs a complete Mass Snapshot and ordinary prepared Behavior inputs still include complete per-tick contexts.
[INFERRED][HIGH] The next slice should make Friendly Behavior context/source publication incremental, rerun the external dual-end recycle gate to completion, and only then migrate the remaining Mixed/Target/Projectile inputs and delete the Legacy Boundary transaction shell.

## 2026-08-02 WA8 Friendly Behavior incremental closure

[COMPUTED][HIGH] Friendly ordinary intents no longer encode complete per-entity Behavior contexts. Clock schedules local Behavior for every live Worker entity and refreshes kinematics from Worker Movement; only contexts with typed external records are transported.
[COMPUTED][HIGH] Sparse prepared expectations validate only affected entities. Context-free autonomous expectations still capture ordered Source/Business events. Friendly commits Worker-owned Source/Resolved/Business state behind the unchanged GT transaction token rather than requiring asynchronous Worker kinematic hashes to equal the Legacy Prepared hash.
[COMPUTED][HIGH] `MassCrowd.BehaviorSource.WorkerAuthoritativeSparse` covers sparse two-entity matching, authoritative kinematic hash divergence, staged-command consumption, and duplicate commit rejection.
[COMPUTED][HIGH] Server run 9644 and real server+client run 9645 passed the 40-second Friendly gate including Claim, Pickup, same-Plan lifecycle recycle, Fallback, and Deliver. Development Editor DisableUnity, RuntimeV2 31/31, Friendly 2/2, and PostFinalize structure 1/1 passed after diagnostic-log cleanup.
[COMPUTED][HIGH] WA8 remains open because Friendly still uses a complete Legacy Mass Snapshot/Runner for GT business transaction planning, Mixed ordinary input has not migrated, and remaining Target/Projectile deltas plus the Round four-stage shell are still present.
[COMPUTED][HIGH] The previously recommended Mixed ordinary-input migration is closed by the checkpoint below; it is retained here only as the predecessor decision, not as current work.

## 2026-08-02 WA8 Mixed ordinary intent closure

[COMPUTED][HIGH] Mixed bootstrap uses one complete BoundarySnapshot; ordinary Worker frames now call `SubmitIntentBatch` and do not pass the per-frame Legacy Snapshot into Worker input.
[COMPUTED][HIGH] Accepted Mixed lifecycle operations append bounded Despawn, Spawn, and Movement Profile Revision records. Runtime acceptance is the acknowledgement boundary. Worker Despawn uses a 1-based transport reason because the gameplay enum is zero-based while Worker `ReasonId` reserves zero.
[COMPUTED][HIGH] Mixed Production commits Worker-authoritative Behavior output after lifecycle/input/fixed-step/event/token validation; it no longer requires asynchronous Worker kinematic or Source-output hashes to equal the GT Prepared mirror.
[COMPUTED][HIGH] Run 9651 accepted the first Despawn at step 226 and Spawn/Profile Revision at step 271, then passed at step 600 with spawn/despawn=`1/1`, stale reject=`0`, Worker submitted=`600`, and zero `VIOLATION`. Development Editor DisableUnity, Mixed Architecture 1/1, and `WorkerAuthoritativeSparse` 1/1 passed.
[COMPUTED][HIGH] WA8 remains open. The next fixed slice is remaining Target/Projectile input delta migration; only after it closes may the Legacy Runner/WorkGraph, Round four stages, mailbox, and `PollBoundaryWork` be physically deleted.

## 2026-08-02 WA8 T8 LocalPredictive / Particle failure closure

[COMPUTED][HIGH] The first deterministic T8 failure was not a Particle solver defect. Exact immutable-input replay captured LocalPredictive step 416 with candidate hash `2662689854`: Agent 19's initial independent result was valid at velocity `(-95,-33)`, but pair completion expanded the connected component to all 20 agents and left one result invalid. The downstream MovementPredict fail-closed velocity then caused the later Particle failures previously observed at steps 474/616.
[COMPUTED][HIGH] `FCrowdMassLocalPredictiveWork` now captures a diagnostic replay only when the ordinary solve is invalid, records the replay CandidateHash/match/valid flags, and exposes the exact trace without adding a second solve to valid production steps. Demo records that failure frame even when the continuous target-stability diagnostic is disabled and builds a minimal witness component fixture on the first invalid step.
[COMPUTED][HIGH] Core and Demo LocalPredictive final pair validation now distinguish a new collision from inherited overlap. A new collision must meet the full required separation; an already-overlapping pair is valid only while its swept closest distance does not become smaller than its starting distance. The same rule is used by final validation, completion safety, coherent translation, and joint blend safety.
[COMPUTED][HIGH] Directed validation passed: Development Editor DisableUnity; `MassCrowd.Core.LocalPredictive` 4/4 including `InheritedOverlapRecovery`; `CrowdDemo.SF.LocalPredictive.PluginCoreEquivalence` 1/1; and `MassCrowd.Runtime.GatherMergeCommit` 1/1 including invalid-work exact replay.
[COMPUTED][HIGH] Real T8 all-Production server run 9742 crossed the previous step-416 failure with zero hard failures. Extended run 9743 reached Worker epoch 771 with Runtime failure 0 and zero LocalPredictive, Particle, Projectile, or other `VIOLATION`, covering the previous step-474 and step-616 failure windows. This is a 20-entity diagnostic/contract closure, not a WA9 scale or dual-PIE acceptance result.
[COMPUTED][MED] Run 9737 observed one Projectile Shadow state-hash mismatch at step 39 with equal state/event counts. The same parameters did not reproduce it in 9741/9742/9743, and 9740 also remained healthy. Failure-only per-projectile field diagnostics are now present, but this single timing-sensitive observation is not closed and must not be reported as a deterministic Projectile kernel defect.
[INFERRED][HIGH] The next slice returns to WA8 structural convergence: remove or migrate the remaining Target/Projectile ordinary-input deltas and complete the Demo-local Round shell deletion gates. Do not add further Particle iteration/line-search work unless a new exact failure trace proves an independent Particle defect.

[RULES I BROKE]：[COMPUTED][HIGH] 无。
