# MassAI Crowd Demo 状态恢复

## 1. 工作区

[COMPUTED][HIGH] 工程：`E:\Projects\SuperInvincibleTank_MASSAITEST`。

[COMPUTED][HIGH] 分支：`codex/t5-target-region-transport`；本轮开始HEAD：`a35e426`。

[COMPUTED][HIGH] 当前工作区包含两版旧架构清理的未提交修改；本轮未执行reset、checkout、clean、stash、stage、commit或push。

## 2. 当前正式边界

[COMPUTED][HIGH] 正式场景只接受`SimRoundObstacle=0`和`SimRoundSoftPressure=1`。旧2/3外部入口、fixed-step构造/调用、地图和历史自动化已经删除。

[COMPUTED][HIGH] SoftPressure当前核心顺序：

```text
RoundPlanApply
→ optional Target/Transport guidance
→ SharedFlowFieldBuild
→ FlowPreferredVelocity
→ LocalPredictiveInteraction
→ MovementPredict
→ ParticleConstraintSolve
→ MovementFinalize
→ AuthorityCommit / ClientPredictionCommit
```

[COMPUTED][HIGH] Target Region Transport只是目标周围空间分布的可选宏观策略；普通移动与窄口不强制使用Transport。Local Predictive和Particle保持场景无关。

## 3. 本轮清理结果

[COMPUTED][HIGH] 旧processor、Pipeline状态、Mass fragment、Rules/Metrics旧字段、SF2–SF4 kernel、Elastic/Joint和RVO2适配已物理删除；旧场景只在parser拒绝测试中保留名称字符串。

[COMPUTED][HIGH] Development与DebugGame Editor通过；Round时间合同与反射目标运动接入后的`CrowdDemo.SF`为34/34成功，生命周期定向6/6、Transport定向6/6；`git diff --check`通过；自动化日志无Fatal、Assertion、Ensure、`LogWindows: Error`或VIOLATION。

[COMPUTED][HIGH] RoundResultHeader版本1 NetSerialize已接入真实复制：代表性T6/Combat为1193/598字节，8645 T1真实头为739字节，8646 T5M为861字节，均低于2048字节。

[COMPUTED][HIGH] Target Region Plan已拆成不可变短期路线与可回滚Quota执行态；短期Agent edge claim只用于保证跨fixed-step一致消费，不是永久Slot/Region owner。

[COMPUTED][HIGH] 新增只读Plan Lifecycle诊断纯kernel、异构cohort聚合、双端hash、rollback和单次fixture输出。诊断关闭时不分配历史且不改变正式结果。

## 4. 当前准确停止点

[COMPUTED][HIGH] 当前正在执行20实体性能与视觉连续性收敛。已加入完整fixed-step阶段计时、追帧/积压/realtime、client visual processor与连续性指标、静态Topology缓存、静态Demand轻量更新、零误差correction快速路径及client-only双SimState插值。

[COMPUTED][HIGH] 用户DebugGame PIE复测约10多FPS后，8696逐阶段证据定位到Shared Flow V2每实体每步两次全Navigation节点attachment搜索；周期性Transport Bellman-Ford与source attachment全刷新形成额外尖峰。已加入稳定Cell→Node缓存、短Plan attachment复用和确定性queue shortest-path。

[COMPUTED][HIGH] DebugGame双端8701同时通过功能、性能和普通视觉连续性门：handoff/band/settled=`20/20`、coverage=`16/16`；fixed-step p95=`9.841ms`、Shared Flow p95=`0.139ms`、client frame p95=`6.748ms`、visual processor p95=`0.126ms`、collapsed p95=`1`、普通/catch-up discontinuity=`0/0`。日志目录：`Saved/CrowdDemo/CrowdDemo_8701_20260718_182014`。

[COMPUTED][HIGH] 8700首异常为无新Sim状态时显示插值单帧追赶`52.147cm`；距离感知插值修复后8701未复现。约`237ms`启动max和Round reset约`699.516cm`仍须独立人工判断。

[COMPUTED][HIGH] 用户随后在单进程DebugGame PIE连续运行T2、T6 Transit与T6 Static，旧版client frame p95约为`25.1/46.7/55.8ms`，证明独立进程性能门不能外推为PIE流畅。视觉processor约`0.12ms`，主要成本来自同一Game Thread串行执行双端fixed-step。

[COMPUTED][HIGH] 已移除Shared Flow V2构图中的逐厘米×Obstacle与blocked-cell×all-edge重复扫描，并为Particle安全量化和零残差closure增加结果等价快路径。8705 T6 DebugGame首步Flow max=`57.473ms`、fixed-step/Local/Particle p95=`13.187/5.923/6.239ms`、realtime=`1.000`；8706 Development对应为`4.132/1.706/1.812ms`，client frame p95=`5.726ms`。完整`CrowdDemo.SF`实际34项均成功。

[INFERRED][HIGH] 当前准确停止点是修复后单进程DebugGame PIE人工复测；独立Development门已通过，但PIE双端估算约`26.4ms`再叠加渲染，尚不能提前宣称稳定高于30FPS。若仍失败，下一步是将纯POD Local Predictive/Particle从GT driver拆到WORK并稳定合并，而不是继续提高追帧预算。

[COMPUTED][HIGH] T1–T8当前20实体CPU性能矩阵已完成；全部fixed-step/client/visual p95低于预声明预算，simulation realtime factor达到1且fixed-step上限命中0。T2/T3/T4/T5S/T5M/T6S/T6M/T7/T8的当前证据目录分别为8679、8683、8684、8685、8686、8689、8681、8690、8691。

[COMPUTED][HIGH] T6M 8681已把19次不连续全部归类为catch-up，steady discontinuity=0；fixed-step/client/visual p95=`14.587/14.452/0.092ms`。动态异构目标仍有Topology build=`9457`，当前20实体预算通过但不能外推100/500。

[COMPUTED][HIGH] T1 8682出现113次非catch-up跳变；该测试的staging↔active阶段本身通过`BoundaryResetLocation`原子改写位置，不能归因成普通移动插值不足。T6A 8687出现17次非catch-up跳变；T6S 8688/8689分别出现19/0，尚无稳定fixture。下一步应固定最早跳变事实并区分测试边界reset、correction分类和visual时间合同，不得降低门槛掩盖。

[COMPUTED][HIGH] 异构静态Target的Demand缓存已补齐：T6S 8689为Topology build/hit=`7/6300`、Demand full/update=`7/6300`，五类Transport hash和aggregate能力保持一致。Target Region Transport定向测试6/6通过。

[INFERRED][HIGH] Round reset的约700cm跳变及各场景约220–310ms启动/首轮max仍须单独人工判断；steady p95通过不能改写为编辑器启动全程无卡顿。

[COMPUTED][HIGH] 当前20实体通过：T1双端hash与6阶段、T2 handoff/band/settled 20、T3 20/20、T4 20/20、T6 Transit 20/20、T6 Static aggregate 20/20、T8 Projectile 50次命中。

[COMPUTED][HIGH] 当前20实体未关闭：T5 Static 8653为inside20、coverage16/16但`settling_steps=-1`；T5 Moving旧证据为inside20、coverage12/12且稳定窗口0；T6 Moving已在8670以30+15秒合同连续两轮达到inside/coverage 20/20，但当前版人工审片未完成。

[COMPUTED][HIGH] 8654纠正旧结论：移动Target位置变化，但TargetRevision固定为1，TargetRevision重建为0。2010次原因闭合为`229/0/1559/0/31/184/7`；graph变化为cost-only/cell/edge=`1208/351/351`，提前重建1773，Plan age p50/p95/max=`1/15/15`。

[COMPUTED][HIGH] active/geometry eligible/new-plan eligible/dropped-still-feasible claim=`1080/1033/851/1033`；execution invalid=`7/41/0/392/3`。双端生命周期hash=`3232819302`，fixture hash=`2766809658`，correction rollback=`53/0/0/64`，checkpoint与interval error p95=`0cm`。

[COMPUTED][HIGH] fixture路径：`Saved/CrowdDemo/CrowdDemo_8654_20260718_114613/target_region_plan_lifecycle_fixture.json`；step 323、缺失Region 3、旧claim仍可继承，同时出现Supply无可用出口。

[COMPUTED][HIGH] 已实施组合生产合同：cost-only变化不废弃短Plan、替换时迁移合法claim、冻结claim后按当前Demand重算剩余quota并原子发布。定向自动化与完整`CrowdDemo.SF`均通过。

[COMPUTED][HIGH] 8658重建883次，原因=`238/0/351/0/51/236/7`；claim active/geometry/supply/new-plan/migrated/completed/dropped=`520/480/346/346/346/115/0`，生命周期双端hash=`2238624409`，fixture hash=`4171107798`，rollback=`53/0/0/100`，error p95=`0cm`。安全与同步门通过，但coverage仍为19/20。

[COMPUTED][HIGH] 新fixture路径：`Saved/CrowdDemo/CrowdDemo_8658_20260718_130308/target_region_plan_lifecycle_fixture.json`；step 1、缺Profile `2217961739`的Region 13，记录3个Supply无可用出口。全部Supply有效claim已迁移，当前没有有效claim丢失。

[COMPUTED][HIGH] 8659新增最终Region直接fixture选择。连续两轮均为inside20、coverage19，缺Profile `2217961739`的Region 13；生命周期hash=`2238624409`、fixture hash=`4114446767`，两轮一致。Round 1/2 rollback=`53/0/0/76`与`52/0/0/52`，初态hash一致，Particle安全错误0。

[COMPUTED][HIGH] fixture路径：`Saved/CrowdDemo/CrowdDemo_8659_20260718_132242/target_region_plan_lifecycle_fixture.json`；step 883、selection kind 1、Region 13。旧/新Plan均通向Region 13，有效claim成功迁移；旧执行态在变化后的图上有1个Supply无出口，新Plan同boundary补出出口。

[COMPUTED][HIGH] Region 13最后90步的Demand/Plan/Guidance/retention gap=`0/4/81/51`，进入/离开=`40/38`。当前不是Demand缺失、Plan无路或claim丢失；剩余归因位于跨图Guidance连续性与terminal retention/实际行程时间之间。

[COMPUTED][HIGH] 8659 fixture的跨图attachment连续与terminal冻结两个只读反事实已完成；全过程未修改Transport/Guidance/Particle生产行为。

[COMPUTED][HIGH] 8661已完成两个只读反事实：Region 13缺失73步且73步全部存在合法多段在途实体；attachment recovered guidance=0，terminal hold/recovered/final held=`0/0/0`，population violation=0，outcome=`Neither`，双端hash=`3886489997`。

[COMPUTED][HIGH] 最终在途实体Agent 5剩余2条edge，相对目标速度`301.496cm/s`；最后90步剩余edge最小/最大=`2/9`，下降/增加/保持=`7/1/64`。Particle、Transport、双端同步和rollback安全门保持通过，但coverage仍为19/20。

[COMPUTED][HIGH] T6M Round Plan现显式记录名义30秒与15秒宽限。8662/8665证明旧单向移动目标在延长后会越出FlowBounds；现已使用通用有界反射直线运动，前30秒轨迹不变，到安全边界后反向。

[COMPUTED][HIGH] 8671连续两轮均为inside/coverage=`20/20`，7个Capability Profile逐项满足`coverage=min(agent_count, feasible_regions)`；input hash=`2573893896`、initial hash=`3920329045`、Mass AgentState hash=`1956985324`、dynamic-flow round hash=`2219477909`、Particle candidate hash=`2904708453`、Transport五类hash和生命周期hash=`2949210181`跨轮一致。

[COMPUTED][HIGH] 8671两轮Particle Hard/Swept/Obstacle/Bounds与invalid/fallback为0，Plan/Guidance unrouted和Transport invalid/validation为0；客户端checkpoint/interval error p95=`0cm`、cross-round growth=`0cm`，Round 2 rollback=`79/0/0/79`且pass=1。日志目录：`Saved/CrowdDemo/CrowdDemo_8671_20260718_143438`。

[INFERRED][HIGH] 当前停止点转为T5 Static/Moving连续稳定窗口和当前版人工审片。T6M严格30秒仍是历史19/20，不能把45秒通过改写成30秒通过；跨轮确定性使用只包含实际RoundSim实体的`AgentStateHash`，Particle applied hash还包含非Mass目标代理。

## 5. 禁止外推

[COMPUTED][HIGH] 本轮已运行清理后二进制的20实体技术回归，但没有人工审片、100/500或录像。

[INFERRED][HIGH] 不得把34/34纯自动化、coverage单帧通过或安全门通过解释为T5稳定落位、人工视觉门或100/500规模能力已经通过。

## 6. 2026-07-18 T6出口与客户端标记状态

[COMPUTED][HIGH] client-only验收标记已接入：真实TargetSlotLayout绘制精确Slot；当前Target Region Transport绘制最小/最大距离环、Region边界和可行terminal cell，不伪造per-agent最终站位。

[COMPUTED][HIGH] T4/T6 Transit新增群体出口稳定合同：全体完成后停止Flow推进，连续15步速度不高于10cm/s才记final settled。定向自动化4/4、完整`CrowdDemo.SF` 34/34、Development与DebugGame Editor均通过。

[COMPUTED][HIGH] 8707 T6 Transit目录为`Saved/CrowdDemo/CrowdDemo_8707_20260718_195326`；completed/final-settled=`20/20`，group completion/settled step=`298/329`，progress hash=`3541423868`双端一致，安全、rollback与位置误差门通过。

[COMPUTED][HIGH] 8708 T6 Static目录为`Saved/CrowdDemo/CrowdDemo_8708_20260718_195521`；Target Stability 90步窗口inside minimum=`20`、coverage=`20/20`、terminal chatter=0、position peak-to-peak p95/max=`0/0cm`，stability hash=`2774153237`双端一致。

[INFERRED][HIGH] 当前准确停止点是可见客户端PIE人工确认站位标记并完整录制T6 Static。离屏技术运行已通过，但不能替代颜色、位置可读性及实体终态对应关系的人工验收。

[RULES I BROKE]：[COMPUTED][HIGH] 无。

## 7. 2026-07-18 T6A组合落位与朝向最新状态

[COMPUTED][HIGH] 当前分支已实现T6A `Corridor Transit → Target Region Transport → natural settle`。旧8707出口保持只作为分阶段证据；正式T6A能力门同时要求corridor与各Capability Profile的inside/coverage。

[COMPUTED][HIGH] 8716只读诊断定位Profile `2372223050`的Agent 4/5虽已被Demand标为Supply，仍因EngagedHold冻结在Region 9，形成`3人/需求1人`并使coverage停在18/20。生产规则已改为超额Supply使保持资格失效；目标靠近本身仍不会触发主动后退。

[COMPUTED][HIGH] 8719目录为`Saved/CrowdDemo/CrowdDemo_8719_20260718_213253`：wall/corridor/completed=`20/20/20`，inside/coverage=`20/20`，max Region population=`1`，T6 valid=`1`；Transport五类hash双端一致，Particle安全违规与invalid/fallback为0，客户端rollback=`80/0/0`且80次零误差快速路径，checkpoint/interval/yaw/velocity error p95=`0`，agents=visible=`20/20`，fixed-step p95=`3.278ms`、realtime=`1.001`。

[COMPUTED][HIGH] 新增`CrowdDemoFacingKernel`和Facing Resolve processor：最终稳定前朝自主Preferred，Local Predictive/Particle/Obstacle修正不影响自主朝向；连续15步终态稳定后才朝目标，角速度上限360度/秒。Development、DebugGame Editor与完整`CrowdDemo.SF` 35/35通过。

[INFERRED][HIGH] 当前准确停止点是可见PIE人工审片，重点检查Region标记、Ranged目标靠近不后退、移动中朝自主方向、最终稳定后朝目标及转向平滑度。本轮未运行100/500，也未实现新的ORCA或场景专用避让。
