# MassAI Crowd Demo 状态恢复

## 1. 当前工作区

[COMPUTED][HIGH] 工程：`E:\Projects\SuperInvincibleTank_MASSAITEST`。

[COMPUTED][HIGH] 当前分支：`codex/t5-target-region-transport`；当前HEAD：`668f317`。

[COMPUTED][HIGH] 工作区包含大量用户既有及本阶段未提交修改；本阶段没有执行reset、checkout、clean、stash、stage、commit或push。

## 2. 当前正式边界

[COMPUTED][HIGH] 正式基础场景仍包含`SimRoundObstacle=0`与`SimRoundSoftPressure=1`。旧场景2/3及其生产路径尚未删除，因为T6M能力门未通过。

[COMPUTED][HIGH] SoftPressure/Target运行链当前为：

```text
RoundPlanApply
→ TargetFactApply
→ SharedFlowFieldBuild / Dynamic Integration Update
→ FlowPreferredVelocity
→ TargetPolarTopologyBuild
→ TargetRegionPopulationBuild
→ TargetRegionTransportSolve
→ TargetRegionGuidance
→ MovementPredict
→ ParticleConstraintSolve
→ MovementFinalize
→ AuthorityCommit / ClientPredictionCommit
```

[COMPUTED][HIGH] MovementFinalize仍是RoundSimState唯一写入点；Coordinator只负责Plan/Result/checkpoint/correction和紧凑指标。

## 3. 本阶段已实现

[COMPUTED][HIGH] 增加显式`RoundStartPolicy`与`CrowdDemoRoundInitialStateKernel`；所有Demo验证Round均使用`ResetToStableInitialState`，Round 1/2不再依赖`Revision==1`。

[COMPUTED][HIGH] 修复Round结果冻结、Plan预发布/激活和静止boundary claim顺序，Round 2可以正常激活并完整结束。

[COMPUTED][HIGH] Shared Flow V2已拆出Topology、Goal Anchor与Integration接口；动态目标anchor cell改变时重建Integration，世界Obstacle/Bounds Topology保持缓存。

[COMPUTED][HIGH] 新增Round input/initial hash、reset/order计数，以及动态Flow topology/anchor/integration/rebuild/round hash，并接入双端比较与SoftPressure rollback。

## 4. 最新通过门与首失败

[COMPUTED][HIGH] Development Editor通过；完整`CrowdDemo.SF`为56/56通过。

[COMPUTED][HIGH] 最新有效运行目录：`Saved/CrowdDemo/CrowdDemo_8487_20260717_135101`。

[COMPUTED][HIGH] 8487连续两轮的round input=`4290856233`、initial state=`3920329045`、dynamic Flow round=`905043919`，并且Topology=`4029698379`、anchor=`2706`、Integration=`338742383`、rebuild=`25`；两轮与双端一致。

[COMPUTED][HIGH] 两轮Particle四类安全违规、invalid/fallback、Transport unrouted/validation failure、rollback miss/mismatch、位置误差p95与跨轮扩散均为0；agents/visible=`20/20`；日志无Fatal、Assertion、Ensure、`LogWindows: Error`或VIOLATION。

[COMPUTED][HIGH] 首失败是T6M能力门：两轮inside-band均为`17/20`，可行Region coverage=`16`，未达到aggregate=`20/20`。因此没有执行独立Round 1双进程、DebugGame、架构删除或清理后回归。

## 5. 复现命令

[COMPUTED][HIGH] 8487服务端核心命令为：

```text
MassAICrowdDemo.uproject /Game/Maps/CrowdDemo_SimRoundSoftPressureT6HeterogeneousTargetMovingSmall -server -port=8487 -NullRHI -log -CrowdDemoEntityCount=20 -CrowdDemoScenario=SimRoundSoftPressure -CrowdDemoDurationSeconds=30 -unattended -NoSound -CrowdDemoRequireClientReady -CrowdDemoReadyLeadSeconds=3 -CrowdDemoReadyTimeoutSeconds=60 -CrowdDemoRequireParticleCorrectionReplay
```

[COMPUTED][HIGH] 客户端连接`127.0.0.1:8487`，使用相同entity/scenario/duration/readiness/correction replay参数。

## 6. 下一步与禁止外推

[INFERRED][HIGH] 下一任务只应诊断三个未进入有效距离带实体的CapabilityProfile、attachment、Transport终端Region、guidance与settle状态；获得唯一证据后再设计单一修复。

[COMPUTED][HIGH] 当前不得删除SF3/SF4或旧2/3入口，不得宣称T6M通过，不得启动GT/WORK迁移、RoundResultHeader收缩、Projectile插件、T9/T10、100/500、自由游荡或动态刷怪。

[COMPUTED][HIGH] 不得用直接追目标、Particle调参、网络调参或场景特判掩盖剩余能力缺口。

[RULES I BROKE]：[COMPUTED][HIGH] 无。

## 7. 2026-07-17 后续归因增量

[COMPUTED][HIGH] 8487之后新增的正式修复是：Demand phase先投影到稳定可行Region ordinal，以及Guidance对Plan edges稳定排序。对应纯测试、Development与完整`CrowdDemo.SF`均通过。

[COMPUTED][HIGH] 当前权威T6M能力运行是8492：inside-band=`19/20`、coverage=`19`；安全、Transport validity、rollback、双端hash、误差和20实例显示均通过。

[COMPUTED][HIGH] 唯一失败实体Agent15为LargeHeavy Melee，距目标`308.3cm`，上界`300cm`；cell32与next cell16可行且attachment成功。同cohort Agent14从cell31、Agent15从cell32同时指向next cell16，聚合guidance在Particle安全闭环后无法提供Agent15正径向进展。

[COMPUTED][HIGH] 8493的临时next-cell单准入实验没有通过并已撤销；当前Source中不存在`TransportWaiting`生产模式。

[INFERRED][HIGH] 下一步先增加只读稳定窗口诊断，区分同next-cell容量争抢、`TerminalStay↔Supply/Transport` chatter与Particle未收敛；随后只设计并测试聚合Flow merge的时间化容量/执行合同。T6M仍未通过，所有架构删除与插件迁移禁令继续有效。

## 9. 2026-07-17 Target稳定窗口诊断结果

[COMPUTED][HIGH] 已新增纯C++ Target稳定窗口诊断、定向自动化、SoftPressure rollback、RoundResult紧凑指标和server/client hash比较；Development通过，完整`CrowdDemo.SF`为57/57。

[COMPUTED][HIGH] 当前最新有效诊断运行是8496：最后90步inside-band最低17、coverage 16/20、同next-cell争抢90/90步、2个实体连续阻塞90步、Terminal chatter=0、Particle settled window=0；首witness为step825、Agent14、next cell16。诊断hash=`4081220932`双端一致，无安全或同步VIOLATION。

[INFERRED][HIGH] 当前唯一观测首因仍记为`MergeCapacity`，但架构解释已修正：它不是T6专用Cell入口锁，而是Shared Guidance与Particle之间缺少通用局部预测速度合同。Particle未收敛只记录为结果，不作为并列首因。

## 10. 2026-07-17 Local Predictive Interaction 设计对齐

[COMPUTED][HIGH] 当前SoftPressure生产链没有执行局部ORCA/轨迹速度求解；旧DeterministicORCA只服务历史场景2/3，并混有PortalAdmission、PassingBand和SF4 route语义。

[INFERRED][HIGH] 当前权威目标链改为`Shared Guidance → Local Predictive Interaction → MovementPredict → Particle Safety`。新层只读取邻域位置、速度、Preferred、半径、MaxSpeed和可回滚等待事实，不识别T3/T4/T5/T6、地图、窄口或Target语义。

[INFERRED][HIGH] Navigation Cell不增加固定“进入”控制。几何允许多人同时安全前进时全部通过；共同可行域不足时才使用`BlockedAge → 进展缺口 → AgentId`有限期让行，且不创建永久Cell owner。

[COMPUTED][HIGH] 已从旧ORCA提取无场景语义的half-plane数值内核，并新增LocalPredictive kernel、LocalVelocity fragment、processor、prepared SoA、hash、RoundResult和rollback；未直接启用旧ORCA processor。

[COMPUTED][HIGH] Development Editor、LocalPredictive 7/7和完整`CrowdDemo.SF` 64/64通过。8507 T3达到双侧completed=`10/10`，8509 T4达到wall/corridor/completed=`20/20/20`；LocalPredictive均为901/901、invalid=0且双端hash一致。

[COMPUTED][HIGH] 8515 T5 Static中Particle安全、rollback、20/20有效距离带及Transport unrouted/invalid/validation均通过，但可行Region覆盖仅`14/16`。Region 4/5的Demand和quota路径存在；供给Agent 8/16的宏观Guidance非零，Local/Predict速度为约13.45/9.06cm/s，Applied为0。

[COMPUTED][HIGH] 30Hz、1cm位置量化的执行门为15cm/s。8515双端报告`sub_quantum_supply=2`、首Agent 8、诊断hash=`2690604116`一致；Development和完整`CrowdDemo.SF` 62/62通过。

[COMPUTED][HIGH] 8517已固化最终8实体/8 pair fixture，hash=`2500233546`双端一致；低速发生在独立half-plane/补全pair阶段，`CommonVelocity`没有执行。同一fixture证明保持相对速度的共同平移可把供给forward提高到60cm/s以上且保持安全。

[COMPUTED][HIGH] 已接入通用CoherentTranslation。8518安全、双端hash、20/20显示与误差门通过，覆盖由14/16提高到15/16、sub-quantum supply由2降到1；剩余Agent 5 Local约9.22cm/s，Applied=0。

[COMPUTED][HIGH] 8518六实体fixture证明Agent 5/19存在前向超过200cm/s的联合安全解；已接入通用`JointPreferredRecovery`，Development、LocalPredictive 7/7与完整SF 64/64通过。

[COMPUTED][HIGH] 8521 T5 Static原P0单轮达到inside-band=`20/20`、coverage=`16/16`、sub-quantum supply=0；最终90步Target-relative speed和位置峰峰值p95/max均为0，Particle安全、LocalPredictive/Transport双端hash、误差与20实例显示门通过，无VIOLATION。

[INFERRED][HIGH] 当前准确停止点是T5 Static自动化、双端技术和稳定性V1能力门通过，但尚未人工审片。下一任务先录像审片，随后才可运行T5 Moving；不直接进入T6M、删除旧场景2/3或100/500。

## 8. 2026-07-17 Transport站位语义与验收修正

[COMPUTED][HIGH] 文档已明确Navigation Cell是共享通行区域，不是per-agent站位；相同next-cell请求允许存在，但不得无容量控制地让多个实体持续追逐同一anchor点。

[COMPUTED][HIGH] 当前`terminal_settled_count`仅表示当帧`GuidanceMode::TerminalSettle`，并不证明连续静止。8426、8417/8418与8479继续作为旧口径到达、安全和同步证据，但稳定自然落位结论等待新版V1复验。

[INFERRED][HIGH] 新V1要求最终90-step持续inside-band/Region覆盖、merge容量与等待公平、Target-relative低速和位置低抖动、无无因Terminal chatter、Particle settling、安全/hash/rollback与人工审片共同成立。

[COMPUTED][HIGH] 本轮仅修改Markdown；Source、地图、Lighting、Particle与网络参数均未改变。

[RULES I BROKE]：[COMPUTED][HIGH] 无。

[RULES I BROKE]：[COMPUTED][HIGH] 无。
